# HanGo Unit Test Specification & Design Document

**Project Name:** Smart Language Self-Study Platform (HanGo)
**Project Code:** HANGO
**Test Environment Setup Description:**
1. **Backend:** Java 21, Spring Boot 3.x, JUnit 5, Mockito 5.x
2. **Frontend:** Dart ^3.12.0, Flutter SDK, flutter_test (Widget & Unit)
3. **IDE:** Visual Studio Code / IntelliJ IDEA 2024

> **Nguyên tắc:** tài liệu này là **kế hoạch sống**, không phải báo cáo kết quả. Method List (§1) được đối chiếu trực tiếp với `hango-backend/src/main/java/...` tại thời điểm cập nhật gần nhất (2026-07-13) — không suy từ mô tả trong `HanGo_Documentation.md`/`doc/specs/0X-*.md`, vì dự án thay đổi nhanh hơn tài liệu (xem [`TESTING.md`](../TESTING.md) §0). Mọi test case trong §2/§3 mặc định **Untested** cho tới khi thật sự được viết và chạy — không tự đánh dấu Passed.
>
> **Cập nhật 2026-07-17 (chỉ đạo tester):** code là ưu tiên số 1 — khi code thay đổi, các file markdown (kể cả `HanGo_Documentation.md`) được chủ động sửa lại cho khớp thay vì chỉ gắn cờ "gap" chờ dev xác nhận. Unit test **chỉ tập trung ở Service layer**, không viết/duy trì test Controller layer nữa (ngoại lệ: class không có Service riêng, business logic nằm thẳng trong Controller — vẫn giữ). File test được tổ chức/xóa tự do miễn không thừa/trùng. UTC tracking dùng **CSV dạng phẳng, 1 testcase = 1 dòng** (không dùng định dạng wide/transposed nữa).

---

## 1. Method List (grounded in current source code)

Trạng thái: **Implemented** = class/method tồn tại thật trong code · **Planned** = module chưa có Service backend, tên method là đề xuất theo convention hiện có (camelCase, không hậu tố `Async` — khác quy ước `.NET` của dự án tham khảo) · **Verify** = tồn tại nhưng hành vi/role-gating cần xác nhận lại khi viết test.

| # | Module | Class | Method Name | Signature (tham số → trả về) | Status |
|---|---|---|---|---|---|
| 1 | AUTH | AuthService | `authenticateUser` | `LoginRequest` → `LoginResponse` | Implemented |
| 2 | AUTH | AuthService | `registerUser` | `RegisterRequest` → `UserResponse` | Implemented |
| 3 | AUTH | AuthService | `googleLogin` | `GoogleLoginRequest` → `LoginResponse` | Implemented |
| 4 | AUTH | AuthService | `forgotPassword` | `ForgotPasswordRequest` → `void` | Implemented |
| 5 | AUTH | AuthService | `verifyOtp` | `VerifyOtpRequest` → `void` | Implemented |
| 6 | AUTH | AuthService | `resetPassword` | `ResetPasswordRequest` → `void` | Implemented |
| 7 | AUTH | AuthService | `verifyAccount` | `email` → `void` | Implemented |
| 8 | AUTH | AuthService | `resendVerificationEmail` | `email` → `void` | Implemented |
| 9 | AUTH | AuthService | `isAccountVerified` | `email` → `boolean` | Implemented |
| 10 | AUTH | UserDetailsServiceImpl | `loadUserByUsername` | `email` → `UserDetails` | Implemented |
| 10b | AUTH | — | `refreshToken` / `logout` | — | **Gap, not Planned-and-fine**: `HanGo_Documentation.md` §5 promises "access token + refresh token", but only a single JWT is issued anywhere in the codebase — no refresh/revoke endpoint exists. Raise with dev team before assuming this is just unbuilt-as-designed. |
| 11 | PROF | AuthService | `getUserProfile` | `email` → `UserResponse` | Implemented *(no dedicated ProfileService — lives in AuthService)* |
| 12 | PROF | AuthService | `updateProfile` | `email, ProfileUpdateRequest` → `UserResponse` | Implemented |
| 13 | PROF | AuthService | `changePassword` | `email, ChangePasswordRequest` → `void` | Implemented |
| 14 | PROF | AuthService | `updateAvatar` | `email, MultipartFile` → `UserResponse` | Implemented |
| 15 | PROF | AuthService | `getUserById` | `id` → `UserResponse` | Implemented |
| 16 | PROF | — | Learning Profile aggregation (FR-PROF-04) | — | Planned *(no endpoint found)* |
| 17 | PROF | — | Trainer public/brand profile page (📌 FR-PROF-05) | — | Planned |
| 18 | RBAC | AdminController | `getDashboardStats` | — → stats map | Implemented — tested 2026-07-22 (2 tests, pure Mockito unit test — no MockMvc needed, it's a plain method call with no request binding to exercise). Extended with `totalCourses`/`totalEnrollments`/`topCourses` (FR-RBAC-06). **Real bug fixed:** previously fabricated positive weekly-registration numbers whenever no real signup happened in the last 7 days (`totalUsers/7`-based), indistinguishable from genuine data — removed, weeks with no signups now correctly show zeros. |
| 19 | RBAC | AdminController | `getUsers` | filters/paging → page of users | Implemented — tested 2026-07-22 (5 tests): role-type filter (incl. `course_manager`/`trainer_lead` alias), search, staff-default, pagination. |
| 20 | RBAC | AdminController | `getUserDetail` | `id` → `UserResponse` | Implemented (delegates `AuthService.getUserById`) — tested 2026-07-22 (2 tests). **Real bug fixed:** returned 400 instead of 404 when the user didn't exist (generic `catch (Exception e)` swallowed `UsernameNotFoundException`) — now special-cased to 404. |
| 21 | RBAC | AdminController / AuthService | `createUserByAdmin` | `RegisterRequest` → `UserResponse` | Implemented — tested 2026-07-22 (2 Controller tests + 6 `AuthServiceTest` Service-layer tests). **Real bug fixed:** `AuthService.createUserByAdmin` accepted *any* non-empty role string and, if it didn't already exist, silently created a new garbage `Role` row on a typo (`roleRepository.save(new Role(typo))`) — now whitelisted to `LEARNER`/`TRAINER`/`TRAINER_LEAD`/`ADMINISTRATOR`. Controller now also writes an `AuditLog` row on success. |
| 22 | RBAC | AdminController | `updateUserByAdmin` | `id, AdminUserUpdateRequest` → `UserResponse` | Implemented — tested 2026-07-22 (7 tests). **Real bug fixed (the most serious one this round):** this endpoint set `user.setStatus(...)` directly with **zero validation and no self-lock check**, completely bypassing the whitelist + "admin can't lock themselves" protections that `updateUserStatus` (row 23) already enforced — a caller could set an arbitrary status string here, or lock their own account, through this alternate path. Now shares the exact same `ALLOWED_USER_STATUSES` whitelist and self-lock guard. Also now writes an `AuditLog` row when any field actually changes. |
| 23 | RBAC | AdminController | `updateUserStatus` | `id, status` → success map | Implemented — 4 pre-existing tests kept as-is (still Passed 2026-07-22), **1 test updated** to also verify the new `AuditLog` write. Correcting a stale note from the 2026-07-13 survey: this method **already had** the self-lock guard and status whitelist at that time — the "no self-lock guard, no status whitelist" gap actually lived in row 22 (`updateUserByAdmin`), not here; that mislabeling is now fixed. |
| 24 | RBAC | GeminiClientService | AI Usage Logging | `recordUsage(callType, success, durationMs, errorMessage)` → `void` (private) | Implemented (2026-07-22) — **not** an `@Aspect` (the 2026-07-13 survey's guess of the likely design was wrong); it's a private helper called directly at the end of `generateChatResponse`/`generateEmbedding`, since `GeminiClientService` is already the single real choke point for every Gemini call in the app (per its own class comment). Persists to new `AiUsageLog` entity/repository. Exposed via `GET /api/admin/ai-usage` (`AdminController`, tested — see row 18's file, 2 tests). `GeminiClientService` itself remains untested (unchanged mock-boundary decision, see §2 2026-07-19 log). |
| 25 | RBAC | AdminController | Audit Log | `GET /api/admin/audit-log` → list | Implemented (2026-07-22) — new `AuditLog` entity/repository, written by `logAudit()` inside `AdminController` on `createUserByAdmin`/`updateUserByAdmin`/`updateUserStatus` success. Tested (1 test for the read endpoint, plus the write side is covered by rows 18/21/22/23's audit-log assertions). **Deliberately scoped to RBAC/user-management actions only** — does not cover approve/publish/payment actions in other modules (see doc/specs/03-rbac.md §3). |
| 25b | RBAC | — | `getPermissions` / `updatePermissions` | — | **Open item, not a firm Planned target** — `HanGo_Documentation.md` FR-RBAC-05 itself marks this 📌 (undecided: static or dynamic permissions). Confirm scope with team before writing acceptance UTCs. |
| 26 | TRN | TrainerOnboardingService(Impl) | `becomeTrainer` | `email, trainerType` → `LoginResponse` | Implemented — grants TRAINER role/dashboard access immediately, not gated by Admin approval; publish/monetization is still gated separately via `TrainerProfile.status='VERIFIED'` in `TrainerDashboardServiceImpl.publishTrainerCourse` (see BR-TRN-01 in `HanGo_Documentation.md`, revised 2026-07-17) |
| 26b | TRN | TrainerOnboardingService(Impl) | `getTrainerProfile` | `email` → `TrainerProfileDTO` | Implemented — JIT-creates a default profile row if none exists |
| 26c | TRN | TrainerOnboardingService(Impl) | `saveProfileDraft` | `email, TrainerProfileDTO` → `TrainerProfileDTO` | Implemented (replaces the guessed `uploadDocuments` — no separate upload method; file URLs come in as plain string fields on the same DTO) |
| 27 | TRN | TrainerOnboardingService(Impl) | `submitProfileForReview` | `email, TrainerProfileDTO` → `TrainerProfileDTO` | Implemented (replaces the guessed `submitApplication`) |
| 27b | TRN | TrainerOnboardingService(Impl) | `getTrainerProfilesForAdmin` | `search, status` → `List<TrainerProfileDTO>` | Implemented |
| 28 | TRN | TrainerOnboardingService(Impl) | `reviewTrainerProfile` | `userId, TrainerReviewRequest` → `TrainerProfileDTO` | Implemented (replaces the guessed `reviewApplication`/`activateTrainer` — one method handles both approve and suspend via `status`) |
| 29 | CRS | CourseServiceImpl | `getCourses` | `search, filterType, difficulty` → `List<CourseSummaryDTO>` | Implemented — tested 2026-07-17 (part of LRN round, `CourseServiceTest.java`) |
| 30 | CRS | CourseServiceImpl | `getCourseDetail` | `id, currentUserId` → `CourseDetailDTO` | Implemented — tested 2026-07-17 (part of LRN round) |
| 31 | CRS | TrainerDashboardServiceImpl | `getTrainerCourses` | `email, status, search, sortBy, timePeriod` → `TrainerCoursesResponseDTO` | Implemented — tested 2026-07-18 (7 tests) |
| 32 | CRS | TrainerDashboardServiceImpl | `createTrainerCourse` | `email, TrainerCreateCourseRequestDTO` → `void` | Implemented — tested 2026-07-18 (8 tests, incl. category/difficulty key normalization) |
| 33 | CRS | TrainerDashboardServiceImpl | `updateTrainerCourse` | `id, email, TrainerCreateCourseRequestDTO` → `void` | Implemented — tested 2026-07-18 (5 tests, incl. section/lesson diffing). **Confirmed gap (was "Verify", now read+tested):** no `course_versions`/clone-on-edit-published logic exists anywhere — editing any course (Draft OR already-Published) overwrites the same row and its sections/lessons in place. BR-CRS-03/§9.7's "sửa nội dung đã Published → duyệt lại + versioning" is entirely unimplemented, not just unverified. |
| 34 | CRS | TrainerDashboardServiceImpl | `getTrainerDashboardSummary` | `email` → `TrainerDashboardSummaryDTO` | Implemented — tested 2026-07-18 (2 tests) |
| 34b | CRS | CourseManagerDashboardServiceImpl | `getDashboardSummary` | — → `CourseManagerDashboardSummaryDTO` | Implemented — newer class not in the original method list (found 2026-07-18); tested (1 test). Aggregate stats only (registered users, active/inactive course counts, exam count) — not a review/approve action. |
| 35 | CRS | CourseManagerDashboardServiceImpl | `getCoursesForReview` / `getCourseReviewDetail` / `publishCourse` / `returnCourseToDraft` / `getExamsForReview` / `publishExam` / `returnExamToDraft` | various | **Correction 2026-07-24 — row 35 above was wrong, not just stale.** The review/approve/reject/publish workflow **is implemented** (8 public methods total incl. row 34b's `getDashboardSummary`) — the 2026-07-18 "Planned/Gap" note was based on an incomplete read of the class. `publishCourse`/`returnCourseToDraft`/`publishExam`/`returnExamToDraft` already had tests (7, pre-existing). **Newly tested 2026-07-24:** `getCoursesForReview`, `getCourseReviewDetail`, `getExamsForReview` (11 new tests) — these back the actual review-queue UI and had zero coverage until now. **Known inconsistency, not fixed:** `TrainerDashboardServiceImpl.approveTrainerCourse`/`rejectTrainerCourseDraft` (row 40b's class) implement an overlapping-but-different Course approve/reject path with different status values (`REJECTED` vs this class's `DRAFT`) and no notifications — see `AUDIT_REPORT.md` HIGH-04. Which path is actually wired to the frontend is unconfirmed. |
| 36 | CRS | — | Price-tier suggestion (FR-CRS-06) | — | Planned |
| 37 | CNT | LessonServiceImpl | `getLessonDetail` | `lessonId, userId` → `LessonDetailDTO` | Implemented — tested 2026-07-18 (6 tests). Deferred from the LRN round over assumed JdbcTemplate-mocking complexity; turned out simple — the whole `jdbcTemplate.query(...)` call is itself the mock, so the real RowMapper/SQL never runs. |
| 38 | CNT | LessonServiceImpl | `getQuizAttempts` | `lessonId, userId` → `List<LessonQuizAttemptDTO>` | Implemented — tested 2026-07-17 (part of LRN round) |
| 39 | CNT | LessonServiceImpl | `saveQuizAttempt` | `lessonId, userId, LessonQuizAttemptRequestDTO` → `LessonQuizAttemptDTO` | Implemented — tested 2026-07-17 (part of LRN round) |
| 40 | CNT | TrainerDashboardServiceImpl | Section/Lesson CRUD | inline inside `updateTrainerCourse` | Implemented *(not a standalone method — no `SectionService`)* — tested 2026-07-18 as part of row 33 |
| 40b | CNT | TrainerDashboardServiceImpl | `getTrainerExams` / `createTrainerExam` / `saveExamQuestions` / `getExamQuestions` / `updateExamStatus` / `deleteTrainerExam` | various | Implemented — Trainer exam-authoring methods, shared class with rows 31-34/40. Tested 2026-07-18 (14 tests) — closes the deferral first noted in the EXAM round (2026-07-17) and carried through the LEARNING round. Role-gating question from the original EXM survey (row 54, now folded into this row) resolved: `updateExamStatus` has **no role/ownership check beyond "is the creator"** — any authenticated Trainer who owns the exam can set any status string (including effectively self-publishing); there is no separate Course-Manager-approval gate for Exams the way there is for Courses (`publishTrainerCourse`'s `TrainerProfile.status=='VERIFIED'` check). Whether this matches FR-EXM-03's "Course Manager tạo được self-publish" intent (i.e. only Course-Manager-authored exams should self-publish, Trainer-authored ones should need approval) could not be confirmed — `updateExamStatus` doesn't distinguish caller role at all, so if the Trainer flow is *supposed* to require Course Manager approval before Publish, that gate does not exist in code today. Flag to dev, not fixed this round. |
| 41 | CNT | CourseImportService | `importWorkbook` | `trainerEmail, MultipartFile` → `CourseImportResultDTO` | **Implemented (real feature discovery 2026-07-18, previously marked Planned/Gap on 2026-07-13's survey)** — 737-line class, parses `.xlsx` internals as raw XML via `org.w3c.dom` (no Apache POI). **NOT tested this round** — deferred as the clear next priority for a dedicated CNT deep-dive; its size/complexity is well beyond every other method tested so far and deserves a focused round rather than being squeezed in alongside CRS. |
| 42 | QB | TrainerQuestionServiceImpl | `getTrainerQuestions` | `email, type, search, sortBy` → `List<QuestionDTO>` | Implemented — tested 2026-07-19 (7 tests). Dynamic SQL built by hand (union of group+single question queries); tested by capturing the generated SQL string and the RowMapper lambda rather than asserting on a real DB. |
| 43 | QB | TrainerQuestionServiceImpl | `createQuestionBankGroup` | `email, CreateGroupQuestionRequestDTO` → `Map<String,Object>` | Implemented — tested 2026-07-19 (7 tests) |
| 44 | QB | TrainerQuestionServiceImpl | `getQuestionDetail` | `email, id, isGroup` → `CreateGroupQuestionRequestDTO` | Implemented — tested 2026-07-19 (7 tests) |
| 45 | QB | TrainerQuestionServiceImpl | `updateQuestionBankGroup` | `email, id, isGroup, request` → `void` | Implemented — tested 2026-07-19 (7 tests). Confirmed: edit is always full delete+recreate of the group/question's rows, never a field-level diff/merge. |
| 46 | QB | TrainerQuestionServiceImpl | `updateQuestionStatus` | `email, questionId, status, isGroup` → `void` | Implemented — tested 2026-07-19 (6 tests) |
| 47 | QB | TrainerQuestionAIService | `generatePayload` | `CreateTrainerQuestionAIRequestDTO` → `CreateTrainerQuestionAIResponseDTO` | Implemented — tested 2026-07-19 (10 tests). Found dead-code gap (GAP-QB-01): computed category/difficulty fallback defaults are never actually sent to the AI prompt. |
| 48 | EXM | ExamService | `getAllExams` | `status` → `List<ExamResponseDTO>` | Implemented — tested 2026-07-17; non-'PUBLISHED' status values always return empty (GAP-EXM-02) |
| 49 | EXM | ExamService | `getExamAttempts` | `examId, userId` → `List<ExamAttemptResponseDTO>` | Implemented — tested 2026-07-17 |
| 50 | EXM | ExamService | `getMyExamAttempts` | `userId` → `List<ExamAttemptResponseDTO>` | Implemented — tested 2026-07-17 |
| 51 | EXM | ExamService | `saveExamAttempt` | `examId, userId, ExamAttemptRequestDTO` → `ExamAttemptResponseDTO` | Implemented — tested 2026-07-17. Confirmed: `request.getScore()` trusted as client input, not recomputed server-side (GAP-EXM-03). New finding: the `answers` field on the response/list endpoints always comes back empty regardless of what was submitted — `answersJson` is persisted as a JSON array but read back via `Map.class` (GAP-EXM-01) |
| 52 | EXM | ExamResultAnalyzerService | `analyzeLatestExamAttempt` | `ExamAttempt` → `ExamResultAnalysisDTO` | Implemented — tested 2026-07-17 |
| 53 | EXM | ExamResultAnalyzerService | `analyzeLearnerAttempts` | `learnerId, List<ExamAttempt>` → `ExamResultAnalysisDTO` | Implemented — tested 2026-07-17 |
| 53b | EXM | TrainerExamMatrixServiceImpl | `getAllExamMatrices` / `createExamMatrix` / `generateExamFromMatrix` | various | Implemented — newer feature not in the original method list (Trainer builds a question-criteria "matrix" — skill/difficulty/category/quantity per line — then auto-generates a DRAFT Exam by randomly pulling matching questions from the bank, erroring if the bank doesn't have enough). Tested 2026-07-17 (`TrainerExamMatrixServiceTest.java`, 13 tests). |
| 54 | EXM | TrainerDashboardService | `createTrainerExam` / `saveExamQuestions` / `getExamQuestions` / `updateExamStatus` / `deleteTrainerExam` | various | Implemented — **deliberately deferred to the CRS/CNT/QB testing round** (2026-07-17 decision): these methods live inside the same large multi-module `TrainerDashboardServiceImpl` class shared with Course/Content authoring; testing 5 of its ~30 methods now would mean re-mocking the same large dependency graph twice. Still **Verify** when that round happens: role-gating for Course Manager approve/self-publish (FR-EXM-03) inside `updateExamStatus`; whether "publish" is just a status value or needs its own endpoint. |
| 54b | EXM | ExamService | `getExamQuestions` | `examId` → `List<LearnerExamQuestionDTO>` | **Resolved 2026-07-24 — this row's "Gap" was wrong.** The learner-facing method exists (distinct from row 40b's Trainer-scoped `getExamQuestions` on `TrainerDashboardServiceImpl` — same method name, different class). Returns an answer-safe DTO (`LearnerQuestionOptionDTO` has no `isCorrect` field by design). Had zero tests despite existing; **5 tests added 2026-07-24** (empty exam, content/skill/option mapping, skill default to "General", question-group passage included/omitted). |
| 55 | AI | AIAssistantService | `sendMessage` | `learnerId, SendMessageRequest` → `SendMessageResponse` | Implemented — tested 2026-07-19 (16 tests, incl. conversation reuse/fallback, out-of-scope short-circuit, history filtering, suggestedQuestions parsing) |
| 56 | AI | AIAssistantService | `getConversationHistory` | `learnerId` → `List<AIConversation>` | Implemented — tested 2026-07-19 |
| 57 | AI | ScopeGuardrailService | `checkScope` | `Lesson, userMessage[, practiceQuestions]` → `ScopeCheckResult` | Implemented — tested 2026-07-19 (7 tests) |
| 58 | AI | AIPromptBuilder | `buildSystemPrompt` / `buildOutOfScopeFallback` | `Lesson[, questions]` → `String` | Implemented — tested 2026-07-19 (6 tests, pure string-building, no mocks needed) |
| 59 | AI | LessonEmbeddingService | `getOrComputeEmbedding` / `recomputeEmbedding` | `Lesson` → `List<Double>` | Implemented — tested 2026-07-19 (4 tests) |
| 59b | AI | VectorUtil | `toJson` / `fromJson` / `cosineSimilarity` | — | Implemented — pure math utility underlying the whole scope-guardrail decision, not previously catalogued; tested 2026-07-19 (6 tests) |
| 60 | AI | GeminiClientService | `generateChatResponse` / `generateEmbedding` / `checkAvailability` | — | Implemented *(thin wrapper over external API — prefer mocking at this boundary)* — **confirmed 2026-07-19 not practically unit-testable**: builds its own `WebClient` internally via `@PostConstruct` with no injectable seam, every method is a real reactive HTTP call chain to the live Gemini API. This is exactly why every other AI-module class takes it as a mocked dependency instead. Would need WireMock/MockWebServer-style HTTP stubbing (integration-style, different test category) — deliberately out of scope. |
| 60b | AI | — | `buildContext` / `retrieveRelevantChunks` | — | **Confirmed 2026-07-19 (was "Not found", now verified by reading `sendMessage` in full):** no RAG-style chunk-retrieval step exists anywhere. The entire "context" given to the AI is just the Lesson's content text + practice questions, embedded directly as plain text into the prompt (`AIPromptBuilder`) and the scope-check input (`ScopeGuardrailService`) — no vector-store lookup, no chunking. |
| 61 | LRN | LessonServiceImpl | `completeLesson` | `lessonId, userId, isCompleted` → `void` | Implemented — tested 2026-07-17 (9 tests). `getLessonDetail` (JdbcTemplate-heavy, CNT-scoped) deferred to CNT round. |
| 61b | LRN | — | `continueLearning` / `getLearningHistory` / `getLearningProgress` | — | **Not found as standalone methods** — FR-LRN-04/05/07 promise this behavior, but progress % etc. is likely computed inline inside `getCourseDetail`/`getTrainerCourses` rather than a dedicated method. Verify before assuming a unit exists to test in isolation. |
| 62 | LRN | CourseServiceImpl | `enrollCourse` / `unenrollCourse` | `courseId, userId` → `void` | Implemented — tested 2026-07-17 (6 + 2 tests). Confirms same publish-gate pattern as `TrainerDashboardServiceImpl.publishTrainerCourse`: enrollment blocked unless the course creator's `TrainerProfile.status='VERIFIED'`. Full `CourseServiceImpl` (incl. `getCourses`/`getCourseDetail`, CRS-scoped) tested together since it's one small class, not deferred. |
| 63 | LRN | CourseRatingServiceImpl | `getCourseReviews` / `addCourseReview` / `deleteCourseReview` | — | Implemented — tested 2026-07-17 (11 tests), **extended 2026-07-22** (21 tests total) with the Course Rating & Review feature: eligibility gate (enrolled + `Enrollment.status='COMPLETED'`), optional review content, `Course.averageRating`/`totalRatings` caching (recalculated on every add/delete, read by `CourseServiceImpl.getCourseDetail` instead of live-recomputing), low-rating (≤3★) and low-average-crossing (>4.0→≤4.0, once) notifications via `NotificationService` (row 84b). `addCourseReview` is still upsert (one rating *slot* per learner/course, editable, not one-time-immutable) — confirmed frontend (`review_tab.dart`) already ships an Edit-review flow, so resubmission was deliberately kept allowed rather than turned into a hard reject, even though a literal reading of "have not submitted before" could suggest otherwise. |
| 64 | LRN | LearningPathwayService | `generatePathway` | `studentId, PathwayGenerateRequestDTO` → `LearningPathwayResponseDTO` | Implemented — pre-existing dev tests (5) + 1 new test 2026-07-17 (empty-course-catalogue fallback) |
| 65 | LRN | LearningPathwayService | `reroutePathway` / `getMyPathway` / `getPathwayById` / `chatWithMentor` / `applySchedule` / `getScheduleStatus` | — | Implemented — `reroutePathway`/`getPathwayById` had pre-existing dev tests; `getMyPathway`, `chatWithMentor`, `getScheduleStatus`, `applySchedule` happy-path had **zero** test coverage until 2026-07-17 (8 new tests added) |
| 66 | LRN | PathwayGoalMergeService | `mergePreview` / `mergeConfirm` | — | Implemented — pre-existing dev test (1), not touched this round |
| 67 | LRN | PathwayMutationService | `applyFastTrackSkip` / `applyDetourInsertion` | — | Implemented — tested 2026-07-17 (10 tests), previously had zero coverage |
| 68 | LRN | PathwayProgressSnapshotService | `getProgressSnapshot` | `pathwayId, learnerId` → `ProgressSnapshotDTO` | Implemented — tested 2026-07-17 (8 tests), previously had zero coverage. `hasWeakSkillOverlap` is hardcoded `false` — self-documented in code as not-yet-implemented, not a hidden bug. |
| 69 | LRN | PathwayReroutePolicyService | `evaluate` | `ProgressSnapshotDTO` → `PolicyDecision` | Implemented — pre-existing dev tests (3), not touched this round |
| 70 | LRN | PathwayTimeboxingScheduler | `schedule` | (static) → `List<NodeSchedule>` | Implemented *(plain static utility, not a Spring bean — easiest pure-unit target in the whole codebase)* — pre-existing dev tests (2), not touched this round |
| 71 | REC | ExamCourseRecommendationAIService | `recommendCoursesAI` | `examAttemptId` → `ExamCourseRecommendationAIResponseDTO` | Implemented — **tested 2026-07-24** (`ExamCourseRecommendationAIServiceTest`, 7 tests: null/not-found guard clauses, success mapping to existing Courses, markdown-fence stripping before JSON parse, cap at 3 recommended courses, fallback-to-empty on AI exception, fallback-to-empty on invalid-JSON response). Previously had zero coverage despite being marked "Implemented" with no caveat — this was the last of the original 14 modules with no test at all. |
| 72 | REC | ExamResultAnalyzerService | (shared with EXM — feeds Weakness Analysis) | — | Implemented |
| 73 | REC | LearningPathwayService | `generatePathway` (shared with LRN — AI Learning Pathway, FR-REC-04) | — | Implemented |
| 73b | REC | — | `recommendCoursesRule` (deterministic, non-AI rule-based match) | — | **Gap** — `HanGo_Documentation.md` separates FR-REC-02 (rule-based: weak SkillType → tagged Course, no AI) from FR-REC-03 (AI Recommendation). Only the AI version (`recommendCoursesAI`) was found; a pure rule-based fallback (e.g. for when AI is unavailable) doesn't appear to exist yet — confirm with dev team. |
| 74 | PAY | PaymentServiceImpl / CartServiceImpl | `createPayment` (checkout) / cart CRUD (`getCartItems`, `addItemToCart`, `removeItemFromCart`, `clearCart`, `syncCart`) | `PaymentRequestDTO, userId` → `PaymentResponseDTO`; cart methods | Implemented — tested 2026-07-24 (`CartServiceImplTest`, 15 tests). `createPayment` itself (PayOS checkout-link creation) is **not** unit-tested — it calls the live PayOS HTTP API via an inline `new RestTemplate()`, not an injectable client (flagged as a gap, not fixed — would require a production refactor outside this round's scope). |
| 75 | PAY | PaymentServiceImpl | `handlePayOSWebhook` (supersedes the originally-planned VNPay `processVnpayIpn` — actual implementation is PayOS) | `Map<String,Object> payload` → `void` | Implemented — tested 2026-07-24 (`PaymentServiceImplTest`, 7 tests for this method alone): signature verification, confirm/test-request short-circuit, revenue split (70/30 PROFESSIONAL vs 60/40 PEER_TUTOR), auto-enroll + cart cleanup + PurchaseSuccess/NewEnrollment notifications, idempotency on already-SUCCESS payments, FAILED-code handling, skip-enroll-when-already-enrolled. |
| 76 | PAY | MonthlyStatementServiceImpl | `generateMonthlyCutoff` | `periodMonth` → `List<MonthlyStatementDTO>` | Implemented — tested 2026-07-24 (3 tests): groups pending SUCCESS payments by trainer, computes 10% PIT tax + net payout, marks payments `IN_STATEMENT`, fires `StatementReady` notification. Manual/on-demand trigger (`POST /api/v1/course-manager/statements/generate`), not yet an automatic end-of-month cron job. |
| 77 | PAY | MonthlyStatementServiceImpl | `confirmTrainerStatement` | `statementId, trainerId` → `MonthlyStatementDTO` | Implemented — tested 2026-07-24 (3 tests): not-found, ownership check, status→`TRAINER_CONFIRMED`. |
| 78 | PAY | MonthlyStatementServiceImpl | `settleStatement` | `statementId, bankTxnRef, notes` → `MonthlyStatementDTO` | Implemented — tested 2026-07-24 (2 tests): not-found, status→`PAID` with bank ref/notes/timestamp + settlement email. |
| 78b | PAY | MonthlyStatementServiceImpl | `getTrainerRevenueSummary` / `getTrainerStatements` / `getCourseManagerStatements` | — | Implemented — tested 2026-07-24. **Correction, same day:** the original "7 tests" claim for this row overstated `getTrainerStatements`'s own coverage — the 7 tests split as 4 for `getTrainerRevenueSummary` + 3 for `getCourseManagerStatements`; `getTrainerStatements` itself had **zero** tests despite being bundled into this row. **3 tests added** for `getTrainerStatements` specifically (empty list, newest-first ordering, doesn't leak another trainer's statements) — total for this row is now 10. |
| 78c | PAY | PaymentExpirationScheduler | `expirePendingPayments` | (scheduled, no params) → `void` | **Newly identified 2026-07-24** — not in any earlier survey of this document. `@Scheduled(cron = "0 */15 * * * *")`, expires `PENDING` Payments older than 30 minutes. No technical barrier to testing (single repository dependency); had zero tests purely by oversight. **3 tests added** (`PaymentExpirationSchedulerTest`): no-op when nothing stale, marks+saves each stale payment as `EXPIRED`, queries with the correct ~30-minute cutoff. |
| 79 | CMT | CommentServiceImpl | `getCommentsByLesson` | `lessonId, currentUserId` → `List<CommentDTO>` | Implemented — tested 2026-07-22 (5 tests). Confirmed+fixed real bug: was returning the author's own `REJECTED` comment too (only `PENDING` should stay author-visible); `isVisibleTo` now hides `REJECTED` unconditionally. |
| 80 | CMT | CommentServiceImpl | `addComment` | `lessonId, userId, CommentRequestDTO` → `CommentDTO` | Implemented — tested 2026-07-22 (5 tests). Now runs every submission through `CommentRuleEngineService.evaluate` (blacklist keyword/regex/normalize) to set `status`/`normalizedContent`/`detectionReason` — previously always hardcoded `status="APPROVED"` with **no moderation at all** (confirmed real gap, fixed same round). |
| 81 | CMT | CommentServiceImpl | `updateComment` | `commentId, userId, CommentRequestDTO` → `CommentDTO` | Implemented — tested 2026-07-22 (3 tests). Edit also re-runs the Rule Engine (can flip an already-APPROVED comment to REJECTED), closing a bypass where a clean comment could be edited afterward to add a blacklisted word. |
| 82 | CMT | CommentServiceImpl | `deleteComment` | `commentId, userId` → `void` | Implemented — tested 2026-07-22 (3 tests), confirms "own comment only". **Admin moderation** (FR-CMT-04) lives in `AdminCommentController` (not a Service — direct `CommentRepository` access for list/detail/approve-reject/delete), Controller-layer, intentionally not unit-tested per Service-only policy. |
| 83 | CMT | CommentServiceImpl | `likeComment` / `unlikeComment` | `commentId, userId` → `CommentDTO` | Implemented — tested 2026-07-22 (2 tests). |
| 83b | CMT | CommentRuleEngineService | `evaluate` / `normalize` | `String` → `CommentModerationResult` / `String` | Implemented — new class (2026-07-22), not in the original 2026-07-13 survey since it didn't exist yet. Simple hardcoded blacklist-keyword + regex-pattern + text-normalization engine (no AI), per HanGo v1.0 Comment Management spec. Tested (10 tests). **Real bug found and fixed mid-round:** the first version of the blacklist was a sparse placeholder (`dm`, `vcl`, `fuck`, `shit`...) that missed most actual Vietnamese/English profanity, so "bậy bạ" comments were silently `APPROVED` — expanded the list and added a `PENDING`-tier "suspicious keyword" bucket for milder insults. |
| 84 | NTF | NotificationService | `notifyUser` (event triggers: PurchaseSuccess, NewEnrollment, CommentReply, ContentApproved, ContentRejected, StatementReady, CourseUpdated) | `User, type, title, message, Course` → `void` | Implemented — 2026-07-24: expanded `NotificationService` from role-broadcast-only into a full per-user notification store (`Notification.user`, nullable, added alongside the pre-existing `recipientRole`). All 7 trigger types wired directly into the relevant business services (see `doc/specs/14-notification.md` §2 trigger table). Tested (2 tests on `notifyUser` itself + trigger-site assertions inside `CourseManagerDashboardServiceTest`, `CommentServiceImplTest`, `PaymentServiceImplTest`, `MonthlyStatementServiceImplTest`). Still not on `ApplicationEventPublisher`/`@Async` — triggers call the service directly (documented simplification, not a defect). |
| 84b | NTF | NotificationService | `notifyCourseManagers` | `type, title, message, Course` → `void` | Implemented — original minimal class (2026-07-22), built for Course Rating & Review's FR-LRN-10/11 (low-rating / low-average-rating alerts to **Course Manager only**, role `TRAINER_LEAD`). Unchanged role-broadcast mechanism, now living alongside row 84's per-user targeting. Tested (1 test). |
| 85 | NTF | NotificationService + NotificationController | `getNotificationsForUser` / `getUnreadCount` / `markAsRead` / `markAllAsRead` | `userId, roles[, page, size]` → `Page<NotificationDTO>` / `long` / `void` / `int` | Implemented — 2026-07-24: new generic `NotificationController` (`GET/PUT /api/v1/notifications/**`) available to any authenticated role (LEARNER/TRAINER/TRAINER_LEAD/ADMINISTRATOR), on top of the pre-existing TRAINER_LEAD-only pair in `CourseManagerDashboardController` (unchanged, still works). `markAsRead` enforces ownership for per-user notifications and role-membership for broadcasts. Tested (7 tests). Frontend bell (`shared_header.dart`) wired to real data with unread-count badge, replacing the previous static mock. Still Planned: realtime WebSocket push, dedicated full-page notification list (bell dropdown is the only UI surface in v1). |

> **Đề xuất bị loại khi rà lại với code (từ bản mở rộng của tester ngày 2026-07-13):** `register`→dùng `registerUser` (tên thật); `refreshToken`/`logout`→xem hàng 10b (gap, không phải method có sẵn); `assignRole`/`updateRole`→dư thừa, `updateUserByAdmin` đã nhận field `role`, HanGo không có Role CRUD động; `searchCourses`/`filterCourses`→là tham số của `getCourses`, không phải method riêng; `createQuestion`/`updateQuestion`/`deleteQuestion`→tên sai, dùng `createQuestionBankGroup`/`updateQuestionBankGroup`/`updateQuestionStatus`; `generateQuestionByAI`+`generateExplanationByAI`→gộp thành 1 method thật `generatePayload` (sinh câu hỏi+giải thích cùng lúc); `analyzeWeakness`→trùng với hàng 52 (`analyzeLatestExamAttempt`); `generateLearningPathwayRecommendation`→trùng với hàng 64 (`generatePathway`). Các đề xuất còn lại đã được thêm vào bảng trên dưới dạng hàng `Xb` kèm trạng thái Gap/Verify/Planned phù hợp.

---

## 2. Test Priority Plan (~100 UTC target)

Ưu tiên theo [`agent_qa.md`](agent_qa.md) §3 (🔴 Critical / 🟡 Important / 🟢 Normal). Mỗi module được chọn **method đại diện** để lên UTC ngay; các method còn lại trong §1 sẽ được lên UTC ở đợt sau theo cùng nguyên tắc. Trạng thái Passed/Failed/Untested phản ánh **thực tế chưa có test nào được chạy** — toàn bộ để Untested cho tới khi tester thực thi.

| # | Module | Priority | Method | Impl. Status | Passed | Failed | Untested | N | A | B | Planned Total |
|---|---|:--:|---|---|:--:|:--:|:--:|:--:|:--:|:--:|:--:|
| 1 | AUTH | 🔴 | `authenticateUser` | **Done** (2026-07-14) | 6 | 0 | 0 | 1 | 3 | 2 | 6 |
| 2 | AUTH | 🔴 | `googleLogin` | **Done, partial** (2026-07-14) | 1 | 0 | 0 | 0 | 1 | 0 | 1 (+1 blocked, see §3.2b) |
| 3 | RBAC | 🔴 | `updateUserStatus` | **Done** (2026-07-22, whole module) | 31 | 0 | 0 | — | — | — | 31 (all AdminController + createUserByAdmin tests this round, see log below) |
| 4 | RBAC | 🔴 | `getUsers` | **Done** (2026-07-22, folded into row 3's total) | — | — | — | — | — | — | — |
| 5 | EXM | 🔴 | `saveExamAttempt` | **Done** (2026-07-17, 2 real gaps confirmed) | 41 | 0 | 0 | — | — | — | 41 (all EXM Service methods, see log below) |
| 6 | EXM | 🔴 | `analyzeLatestExamAttempt` | **Done** (2026-07-17, folded into row 5's total) | — | — | — | — | — | — | — |
| 7 | PAY | 🔴 | `handlePayOSWebhook` (was `processVnpayIpn`) | **Done** (2026-07-24) | 43 | 0 | 0 | — | — | — | 43 (all PaymentServiceImpl + CartServiceImpl + MonthlyStatementServiceImpl tests this round, see log below) |
| 8 | PAY | 🔴 | `generateMonthlyCutoff` (was `generateMonthlyStatements`) | **Done** (2026-07-24, folded into row 7's total) | — | — | — | — | — | — | — |
| 9 | CRS | 🟡 | `updateTrainerCourse` | **Done** (2026-07-18) | 50 | 0 | 0 | — | — | — | 50 (all new CRS+CNT-module tests this round, see log below) |
| 10 | CNT | 🟡 | `saveQuizAttempt` | **Done** (2026-07-17, part of LRN round) | — | — | — | — | — | — | — |
| 11 | LRN | 🟡 | `completeLesson` | **Done** (2026-07-17) | 74 | 0 | 0 | — | — | — | 74 (all new LRN-module tests this round, see log below) |
| 12 | REC | 🟡 | `recommendCoursesAI` | **Done** (2026-07-24) | 7 | 0 | 0 | — | — | — | 7 (see row 71 in §1 and the log below) |
| 13 | PROF | 🟢 | `updateProfile` | Implemented | 0 | 0 | 3 | 2 | 1 | 0 | 3 |
| 14 | TRN | 🟢 | `submitProfileForReview` | **Done** (2026-07-17) | 32 | 0 | 0 | — | — | — | 32 (all 6 Service methods, see §2 note below) |
| 15 | QB | 🟢 | `createQuestionBankGroup` | **Done** (2026-07-19) | 44 | 0 | 0 | — | — | — | 44 (all QB Service tests this round, see log below) |
| 16 | AI | 🟢 | `checkScope` | **Done** (2026-07-19) | 39 | 0 | 0 | — | — | — | 39 (all AI Assistant Service tests this round, see log below) |
| 17 | CMT | 🟢 | `deleteComment` | **Done** (2026-07-22) | 29 | 0 | 0 | — | — | — | 29 (all CMT Service tests this round — CommentServiceImpl + CommentRuleEngineService, see log below) |
| 18 | NTF | 🟢 | `notifyUser` (was `publishNotification`) | **Done** (2026-07-24, expanded from the 2026-07-22 `notifyCourseManagers`-only baseline) | 11 | 0 | 0 | — | — | — | 11 (`NotificationServiceTest`, see log below; trigger-site behavior additionally covered inside the tests of the services that fire each trigger) |
| | | | | **Sub-total (18 methods)** | *(stale — see note below, not recomputed this round)* | | | | | | |

> **Note 2026-07-24:** rows 7, 8, and 18 above changed from Planned/partial to Done as part of this round's Payment/Revenue + Notification testing push (see the dated log entry below for detail). The Sub-total row's aggregate numbers were **not** hand-recomputed here — the arithmetic that produced the original 102-total isn't fully reconstructable from the table alone (it doesn't match a simple column sum), so overwriting it with a guessed number risked being wrong in a way that's harder to spot than leaving it flagged. Whoever next reconciles this table should recompute the Sub-total from the individual rows' real Passed/Failed/N/A/B figures (including the 43 + 11 = 54 new tests added 2026-07-24 across rows 7/8/18) rather than trusting the old total.

Ghi chú xếp hạng:
- 🔴 Critical (4 module × 2 method, 10+5 UTC): Auth, RBAC, Exam grading, Payment — đúng theo `agent_qa.md` §3.
- 🟡 Important (6 UTC/module): Course versioning (CRS/CNT gộp theo agent_qa.md), Learning progress (LRN), Recommendation (REC).
- 🟢 Normal (3 UTC/module): các module còn lại (PROF, TRN, QB, AI, CMT, NTF).
- Tất cả method đánh dấu **Planned** vẫn được lên UTC (thiết kế test *trước*, để dùng làm acceptance criteria khi code được viết) — đúng tinh thần "coi dự án đã hoàn thành".
- **2026-07-14:** toàn bộ module AUTHENTICATION (không chỉ 2 method đại diện ở hàng 1-2) đã có test thật — 7 method còn lại của AUTH (`registerUser`, `forgotPassword`, `verifyOtp`, `resetPassword`, `verifyAccount`, `resendVerificationEmail`, `isAccountVerified`, `loadUserByUsername`) được cộng thêm **19 test Passed** ngoài phạm vi 18-method ban đầu — xem §3.2 và §3.2b. Sub-total 102 UTC ở trên **không** đổi vì đây là phần vượt kế hoạch (bonus), không thay thế hàng nào khác.
- **2026-07-14 (tiếp):** module PROFILE — cả 5 method thật (`getUserProfile`, `updateProfile`, `changePassword`, `updateAvatar`, `getUserById`; đều sống trong `AuthService`, không có `ProfileService` riêng) đã có **15 test Passed** trong cùng `AuthServiceTest.java`. `getLearningProfile`/`getTrainerPublicProfile` vẫn Planned (chưa có endpoint).
- **2026-07-14 (fix code production, theo yêu cầu tester):** đã sửa 3 bug tìm thấy khi test, kèm test mới cho từng fix (không đổi hành vi nơi khác):
  1. `AdminController.updateUserStatus` — thêm whitelist status (`ACTIVE`/`INACTIVE`, chặn giá trị khác) + chặn Admin tự đổi status chính mình. Test mới: `AdminControllerTest.java` (4 test).
  2. `AuthService.updateProfile` — reset `isVerified=false` khi user đổi email (trước đây giữ nguyên `true` dù email mới chưa xác minh). Test mới: `updateProfileShouldResetIsVerifiedWhenEmailChanges`.
  3. `AuthService.updateAvatar` — thêm validate file avatar (≤2MB, chỉ jpg/jpeg/png) đúng theo spec `02-profile-management.md`, trước đây không giới hạn gì. Test mới: `updateAvatarShouldRejectFileLargerThan2MB`, `updateAvatarShouldRejectUnsupportedContentType`.
  - Đã xóa `HangoBackendApplicationTests.java` (test giả — nuốt exception, không assert gì, theo xác nhận của tester).
- **2026-07-14 (fix vòng 2, theo yêu cầu tester "fix cả bảng"):**
  - Đã tách `GoogleIdTokenVerifier` thành `@Bean` (`GoogleAuthConfig.java`) → `googleLogin` hết bị chặn, có đủ test happy-path (JIT-provision, reuse account, inactive) — không còn `@Disabled`.
  - ~~Đã xóa role chết `TRAINER_LEAD` khỏi `@PreAuthorize` của `uploadAvatar`~~ — **SỬA LẠI 2026-07-16**: đây là nhận định sai dựa trên khảo sát code cũ (lúc đó `TRAINER_LEAD` thật sự chưa xuất hiện ở đâu khác). Sau khi code base cập nhật (tính năng Course Manager được teammate thêm vào), `TRAINER_LEAD` giờ là role **Course Manager thật**, dùng ở 9+ controller (`TrainerDashboardController`, `CourseManagerDashboardController`, `TrainerOnboardingController`...). Đã thêm lại `hasRole('TRAINER_LEAD')` vào `@PreAuthorize` của `uploadAvatar` — nếu không, Course Manager sẽ không tự đổi được avatar, vi phạm FR-PROF-02 (mọi role đăng nhập đều sửa được profile mình).
  - Đã phân biệt HTTP status (404 khi không tìm thấy tài khoản) cho các endpoint không phải bước xác thực: `verifyAccount`, `resendVerification`, `resetPassword`, `uploadAvatar`, `getUserProfile`, `updateProfile`, `changePassword`. **Cố tình giữ nguyên 400 cho `/login` và `/google`** để tránh lộ thông tin tài khoản tồn tại hay không (account enumeration).
  - Đã bổ sung test Controller-layer đầy đủ (happy-path 200 + lỗi) cho toàn bộ endpoint AUTH (`AuthControllerTest.java`, 21 test) và PROFILE (`UserControllerTest.java`, 9 test) — trước đó chỉ có test Service-layer.
  - #1 (refresh token/logout) và #2 (khóa tài khoản sau 5 lần sai) **chưa implement được** — `application.properties.example` có `spring.jpa.hibernate.ddl-auto=validate`, thêm field/bảng mới vào entity mà chưa chạy migration SQL sẽ làm app **crash khi khởi động**. Đã chuẩn bị sẵn SQL migration + kế hoạch code, chờ dev chạy migration trước.
  - Tổng backend hiện tại: `mvnw test` → **108/108 pass, 0 skip**. Riêng AUTH+PROFILE: 81 test (33 Service + 21 Controller cho AUTH; 18 Service + 9 Controller cho PROFILE).
- **2026-07-14 (fix vòng 3 — DTO Validation, theo yêu cầu tester):**
  - Bổ sung 13 test dùng `@WebMvcTest` + MockMvc thật (không phải Mockito unit test thuần) để chạy qua đúng pipeline Spring Bean Validation (`@Valid`) mà unit test Service-layer không chạm tới được: `AuthControllerValidationTest.java` (10 test — `LoginRequest`, `RegisterRequest`, `GoogleLoginRequest`, `ForgotPasswordRequest`, `VerifyOtpRequest`, `ResetPasswordRequest`), `UserControllerValidationTest.java` (3 test — `ChangePasswordRequest` + 1 test phát hiện gap).
  - **Phát hiện thật:** `ProfileUpdateRequest.java` **không có annotation validation nào** (`@Email`, `@NotBlank`...) dù controller dùng `@Valid` — email sai định dạng hiện được chấp nhận (200) thay vì bị từ chối (400). Ghi lại làm bằng chứng (`updateProfileShouldCurrentlyAcceptMalformedEmailBecauseNoValidationAnnotationExists`), không tự sửa (đợt này chỉ được yêu cầu viết test).
  - **Ghi chú kỹ thuật cho lần sau:** `@WebMvcTest` trong Spring Boot 4.0.6 đổi package (`org.springframework.boot.webmvc.test.autoconfigure.WebMvcTest`, không phải `org.springframework.boot.test.autoconfigure.web.servlet` như bản cũ) và cần `@AutoConfigureMockMvc(addFilters = false)` để tránh bị chặn 403 bởi CSRF/security filter khi chỉ muốn test validation.
  - Đã quyết định **bỏ qua Frontend test** theo yêu cầu tester (rủi ro backend ưu tiên hơn dưới deadline gấp) và **bỏ qua #1/#2** (refresh token, brute-force lockout — vẫn chặn bởi `ddl-auto=validate`, chưa có migration).
  - Tổng backend hiện tại: `mvnw test` → **121/121 pass, 0 skip**.
- **2026-07-16 (verification pass toàn diện, theo yêu cầu tester "check kĩ" sau sự cố đổi nhánh git):**
  - Chạy `mvnw clean test` phát hiện 3 vấn đề thật, đều đã xử lý:
    1. **`registerUser` đổi hành vi thật** (commit `92034ec` của teammate `thinhnguyenxuan`, không liên quan tới việc test) — giờ chấp nhận tự đăng ký role `TRAINER` (trước đây luôn ép về `LEARNER`). Đã cập nhật lại test cho khớp thực tế (2 test mới thay 1 test cũ), và gắn cờ **GAP-AUTH-04**: hành vi này bỏ qua toàn bộ quy trình duyệt Trainer Onboarding (FR-TRN-01..06) — nghi ngờ là lỗi ngoài ý muốn, cần báo dev xác nhận.
    2. **`CheckDBApp.java`** (file debug thủ công của teammate, nằm nhầm `src/test/java`, có `@SpringBootApplication` riêng) làm `@WebMvcTest` lỗi "multiple @SpringBootConfiguration". Đã sửa bằng cách ghim `@ContextConfiguration(classes = HangoBackendApplication.class)` trong 2 file test của mình — **không đụng vào file của teammate**.
    3. **Tự sửa sai ở lượt trước**: từng xóa nhầm `hasRole('TRAINER_LEAD')` khỏi `uploadAvatar` vì tưởng là role chết — hóa ra đây là role Course Manager thật, đang dùng ở 9+ controller khác (do teammate thêm tính năng Course Manager sau lúc khảo sát ban đầu). Đã thêm lại.
  - Đọc lại toàn bộ `AuthService.java`, `AuthController.java`, `UserController.java`, `AdminController.java` (không chỉ grep) để xác nhận không còn sai lệch nào khác.
  - Tổng backend hiện tại: `mvnw clean test` → **122/122 pass, 0 skip**. AUTH+PROFILE riêng: 95 test (`AuthServiceTest` 50, `UserDetailsServiceImplTest` 2, `AuthControllerTest` 21, `AuthControllerValidationTest` 10, `UserControllerTest` 9, `UserControllerValidationTest` 3).
  - **Lưu ý mang sang RBAC (module tiếp theo):** `AdminController.updateUserByAdmin` cũng set `status` trực tiếp (`user.setStatus(...)`) mà không qua whitelist `ALLOWED_USER_STATUSES` như `updateUserStatus` — cùng một lỗ hổng, đường đi khác. Cũng phát hiện `TrainerOnboardingController.java` đã tồn tại thật trong code (khác với kết luận "chưa có" ở lần khảo sát đầu) — cần khảo sát lại module TRAINER ONBOARDING từ đầu, không dùng lại kết luận cũ.
- **2026-07-17 (module TRAINER ONBOARDING — theo yêu cầu tester "dev báo luồng đã hoàn thiện, chuyển sang test TRN"):**
  - Khảo sát lại từ đầu (đọc toàn bộ `TrainerOnboardingController.java`, `TrainerOnboardingServiceImpl.java`, `TrainerProfile.java`, `TrainerProfileDTO.java`, `TrainerReviewRequest.java`) — xác nhận module có 6 method Service thật: `becomeTrainer`, `getTrainerProfile`, `saveProfileDraft`, `submitProfileForReview`, `getTrainerProfilesForAdmin`, `reviewTrainerProfile`. §1 hàng 26-28 đã cập nhật từ Planned sang Implemented với chữ ký thật.
  - Viết `TrainerOnboardingServiceTest.java` (32 test) + `TrainerOnboardingControllerTest.java` (16 test, Mockito thuần — module này không có `@Valid` trên DTO nào nên không cần lớp test kiểu `@WebMvcTest` như AUTH/PROFILE).
  - Khi chạy lần đầu, 5 test thất bại — đều là lỗi trong test tự viết, không phải bug code production:
    1. `becomeTrainerShouldUpdateTrainerTypeWhenProfileStillPendingVerification`: test giả định `roleRepository.findByRoleName("TRAINER")` không được gọi khi user đã có role Trainer — nhưng code luôn gọi hàm này vô điều kiện (`becomeTrainer` dòng 42). Sửa lại assertion cho khớp hành vi thật (chuyển sang verify `userRepository.save()` không được gọi, vì đó mới là điểm khác biệt thật khi user đã là Trainer).
    2-5. 4 test trong nhóm `submitProfileForReview` (…RejectMissingBio, …RejectMissingPhoneNumber, …RejectMissingAllCertificationDocuments, …MarkAwaitingApprovalWhenAllRequiredFieldsPresent) bị `NullPointerException` vì `submitProfileForReview` gọi `saveProfileDraft` bên trong, và `saveProfileDraft` luôn gọi `userRepository.save(u)` rồi dùng giá trị trả về — test thiếu stub `when(userRepository.save(any())).thenReturn(...)` nên Mockito trả về `null` mặc định. Đã bổ sung stub còn thiếu ở cả 4 test.
  - Sau khi sửa: `mvnw clean test` → **170/170 pass, 0 skip** (toàn bộ backend). Riêng TRAINER ONBOARDING: 48 test (32 Service + 16 Controller).
  - **Phát hiện thật, không tự sửa (theo nguyên tắc "defer to dev" — tester đã dặn ưu tiên quyết định nghiệp vụ của dev):**
    - **`becomeTrainer` cấp role TRAINER ngay lập tức**, không chờ Admin duyệt hồ sơ — ban đầu gắn cờ GAP-TRN-01 (trái BR-TRN-01). **Rà lại 2026-07-17 (xem log tiếp theo):** đây không phải bỏ qua duyệt hoàn toàn — `TrainerDashboardServiceImpl.publishTrainerCourse` vẫn chặn publish/bán Course trừ khi `TrainerProfile.status='VERIFIED'`. Đã sửa `HanGo_Documentation.md` (BR-TRN-01, §9.2) cho khớp thực tế thay vì để là gap.
    - **`TrainerOnboardingController` bắt lỗi generic → luôn trả 400**, kể cả khi `UsernameNotFoundException` (tài khoản không còn tồn tại) — không có phân biệt 404 như `AuthController`/`UserController`. Chưa sửa code; test Controller-layer sau đó đã bị xóa 2026-07-17 (xem log tiếp theo) nên phát hiện này không còn được cover bởi test tự động, chỉ còn ghi chú lại.
  - Đã tạo `doc/specs/utc-sheet-trainer-onboarding.csv` (định dạng wide/transposed lúc đó — đã đổi lại thành flat 1 dòng/testcase ở đợt 2026-07-17 tiếp theo, xem bên dưới).
- **2026-07-17 (dọn dẹp theo 4 chỉ đạo mới của tester — "code ưu tiên số 1, chỉ test Service, dọn file test, CSV quay lại 1 dòng/testcase"):**
  1. **Code ưu tiên, chủ động sửa doc khớp code:** rà lại 2 phát hiện GAP-AUTH-04/GAP-TRN-01 kỹ hơn — hóa ra `TrainerDashboardServiceImpl.publishTrainerCourse` (dòng ~501-504) đã chặn publish/bán Course trừ khi `TrainerProfile.status='VERIFIED'`, nên việc gán role TRAINER sớm (qua tự đăng ký hoặc `become-trainer`) **không** thực sự bỏ qua duyệt Admin cho phần doanh thu — chỉ là Trainer Dashboard/tạo Draft được mở sớm hơn thiết kế gốc. Đã cập nhật `HanGo_Documentation.md`: FR-AUTH-01 (ghi rõ có thể chọn role Trainer lúc đăng ký), BR-TRN-01 (mô tả lại 2 điểm chặn thật: role sớm + publish muộn), §9.2 state diagram (đổi từ Draft/Submitted/Approved sang đúng tên `TrainerProfile.status` thật: PENDING_VERIFICATION/AWAITING_APPROVAL/VERIFIED/SUSPENDED), và Decision Log (§14). Không còn coi đây là "gap cần báo dev" — là hành vi đã xác nhận và ghi lại.
  2. **Thu hẹp phạm vi unit test về Service-only:** xóa 5 file test Controller thuần túy (`AuthControllerTest.java`, `AuthControllerValidationTest.java`, `UserControllerTest.java`, `UserControllerValidationTest.java`, `TrainerOnboardingControllerTest.java` — tổng 59 test). Giữ lại `AdminControllerTest.java` (4 test) vì `AdminController` không có Service riêng — logic self-lock guard + status whitelist nằm thẳng trong Controller, xóa sẽ mất toàn bộ test cho business logic đó. `mvnw clean test` sau khi xóa → **111/111 pass, 0 skip** (giảm từ 170 vì bớt 59 test Controller).
  3. Không phát sinh việc tách/gộp file test Service nào thêm ở đợt này — cấu trúc hiện tại (1 file test / 1 Service class, method-name divider comment) đã đúng tinh thần "không thừa không trùng".
  4. **UTC CSV quay lại định dạng flat (1 testcase = 1 dòng)**, bỏ định dạng wide/transposed đã dùng trước đó — tester phản hồi "cách mới khó nhìn". Viết lại cả 3 file: `utc-sheet-authentication.csv` (34 dòng: 32 AuthService + 2 UserDetailsServiceImpl), `utc-sheet-profile.csv` (18 dòng + 1 dòng Blocked cho getLearningProfile/getTrainerPublicProfile), `utc-sheet-trainer-onboarding.csv` (32 dòng). Cả 3 file bỏ hẳn block Controller layer (theo mục 2). Cột: Module/Class/Method/UTCID/Test Case/Precondition/Input/Expected Result/Exception/Case Type/Status/Exec Date/Defect ID/Created By/Executed By.
  - Tổng backend hiện tại: `mvnw clean test` → **111/111 pass, 0 skip** (toàn bộ Service-layer, không còn Controller-layer test).
- **2026-07-17 (module EXAM MANAGEMENT — theo yêu cầu tester, sau khi xác nhận AUTH/PROFILE/TRAINER_ONBOARDING ổn):**
  - Khảo sát lại từ đầu theo code thật (không dùng lại khảo sát cũ trong §1 vì có thể đã lệch): `ExamService` (209 dòng), `ExamResultAnalyzerService` (272 dòng), và phát hiện **`TrainerExamMatrixServiceImpl`** — một feature Trainer-authoring mới (build "ma trận" tiêu chí câu hỏi theo skill/difficulty/category/quantity, rồi auto-generate Exam DRAFT bằng cách random câu hỏi khớp tiêu chí từ ngân hàng câu hỏi) — chưa từng có trong §1 bản khảo sát 2026-07-13.
  - Viết 3 file test Service-only: `ExamServiceTest.java` (17 test), `ExamResultAnalyzerServiceTest.java` (11 test, không cần mock vì class chỉ nhận `ExamAttempt` trực tiếp, không tự query DB), `TrainerExamMatrixServiceTest.java` (13 test) — tổng 41 test, tất cả pass ngay lần chạy đầu tiên.
  - **Quyết định phạm vi:** KHÔNG viết test cho 5 method exam-authoring nằm trong `TrainerDashboardServiceImpl` (`createTrainerExam`, `saveExamQuestions`, `getExamQuestions`, `updateExamStatus`, `deleteTrainerExam`) — class này dùng chung cho cả Course/Content authoring (CRS/CNT), mock lại toàn bộ dependency graph của nó bây giờ rồi làm lại lần nữa ở đợt CRS/CNT là lãng phí. Dời sang đợt CRS/CNT/QB.
  - **2 phát hiện thật mới (code-first, chưa từng được ghi nhận trước đây):**
    - **GAP-EXM-01**: `ExamService.mapToAttemptDTO` đọc lại `answersJson` bằng `objectMapper.readValue(answersJson, Map.class)` — nhưng `saveExamAttempt` lại lưu `answersJson` dưới dạng **JSON ARRAY** (qua `enrichAnswers`, trả về `List<Map>`). Đọc array bằng kiểu Map ném exception nội bộ, bị catch âm thầm, mặc định về map rỗng. Hệ quả: **API trả về `answers` luôn rỗng** ở `saveExamAttempt`, `getExamAttempts`, `getMyExamAttempts` — dù dữ liệu chi tiết từng câu (userAnswer/isCorrect/skill/topic) đã được lưu đúng trong DB. Có vẻ là bug thật (type mismatch), không phải thiết kế cố ý — đã ghi lại, chưa sửa (đợt này chỉ viết test).
    - **GAP-EXM-02**: `ExamService.getAllExams(status)` — truyền `status` khác `'PUBLISHED'`/`null`/`'All'` (vd `'DRAFT'`) sẽ luôn trả về **danh sách rỗng**: code query DB theo đúng status đó, rồi lọc lại trong bộ nhớ chỉ giữ `status=='PUBLISHED'` — mâu thuẫn, không bao giờ khớp. Có thể là dead code (hiện chưa thấy caller nào dùng path này), nhưng vẫn là một inconsistency thật trong code.
    - **GAP-EXM-03** (kế thừa từ khảo sát cũ, xác nhận lại không đổi): `saveExamAttempt` vẫn tin tưởng hoàn toàn `request.getScore()` từ client, không tính lại điểm từ đáp án đúng, không validate range 0-10 (BR-EXM-01).
  - Đã tạo `doc/specs/utc-sheet-exam.csv` (42 dòng, định dạng flat 1 testcase/dòng, không có block Controller).
  - Tổng backend hiện tại: `mvnw clean test` → **152/152 pass, 0 skip**.
- **2026-07-17 (module LEARNING MANAGEMENT — theo yêu cầu tester "test tiếp LEARNING"; tester cũng báo trước sẽ có đợt test lại full từ đầu ở giai đoạn cuối vì dự án đổi liên tục — chưa làm ngay, chỉ ghi nhận):**
  - Khảo sát lại từ code thật: module LRN trải trên nhiều class nhỏ (`LessonServiceImpl`, `CourseServiceImpl`, `CourseRatingServiceImpl`, `LearningPathwayService`, `PathwayMutationService`, `PathwayProgressSnapshotService`, `PathwayGoalMergeService`, `PathwayReroutePolicyService`, `PathwayTimeboxingScheduler`). Phát hiện: 4 class (`LearningPathwayService`, `PathwayGoalMergeService`, `PathwayReroutePolicyService`, `PathwayTimeboxingScheduler`) đã có sẵn **15 test do dev (teammate `Hoang20444`) tự viết** (commit `0cdf85d`/`8dec141`/`3c6abe6`), đã pass sẵn — không viết đè, chỉ **bổ sung thêm** test cho các method/nhánh dev chưa cover.
  - Viết mới **74 test** trải trên 6 file:
    - `LessonServiceTest.java` (17 test) — `completeLesson` (LRN, 9 test) + `saveQuizAttempt`/`getQuizAttempts` (về mặt phân loại là CNT nhưng không cần JdbcTemplate nên tiện làm luôn, 8 test). **Cố tình bỏ qua `getLessonDetail`** (CNT, dùng JdbcTemplate + RowMapper thô — phức tạp/ít giá trị hơn khi unit test bằng Mockito) — dời sang đợt CNT.
    - `CourseServiceTest.java` (19 test) — toàn bộ `CourseServiceImpl` (`getCourses`, `getCourseDetail` thuộc CRS + `enrollCourse`/`unenrollCourse` thuộc LRN) vì class nhỏ (226 dòng), test hết một lần đỡ phải mock lại ở đợt CRS.
    - `CourseRatingServiceTest.java` (11 test).
    - `PathwayMutationServiceTest.java` (10 test) — trước đó **0% coverage**.
    - `PathwayProgressSnapshotServiceTest.java` (8 test) — trước đó **0% coverage**.
    - Bổ sung 9 test mới vào `LearningPathwayServiceTest.java` (file có sẵn của dev) cho các method trước đó **chưa có test nào**: `getMyPathway`, `chatWithMentor`, `getScheduleStatus`, `applySchedule` (happy path), và nhánh empty-course-catalogue của `generatePathway`.
  - **Phát hiện thật (không phải bug, là xác nhận thiết kế):** `CourseServiceImpl.enrollCourse` cũng chặn ghi danh trừ khi `TrainerProfile.status='VERIFIED'` — giống hệt cơ chế đã thấy ở `TrainerDashboardServiceImpl.publishTrainerCourse` (đợt TRAINER_ONBOARDING). Củng cố thêm kết luận đã ghi trong `HanGo_Documentation.md` BR-TRN-01: role Trainer được cấp sớm, nhưng mọi hành vi "kiếm tiền được" (publish Course, có học viên ghi danh) đều bị chặn tới khi Admin duyệt.
  - Sau khi thêm: `mvnw clean test` → **226/226 pass, 0 skip**.
  - Đã tạo `doc/specs/utc-sheet-learning.csv` (75 dòng — chỉ liệt kê 74 test mới viết đợt này; 15 test có sẵn của dev được ghi chú tóm tắt, không lặp lại chi tiết UTC vì không thuộc phạm vi thiết kế UTC của tester).
  - **Lưu ý cho giai đoạn cuối:** tester đã báo trước sẽ yêu cầu chạy lại toàn bộ test suite từ đầu (full re-test) khi dự án gần hoàn thiện, vì code thay đổi liên tục theo ngày — chưa cần làm ở đợt này, ghi nhận lại làm việc cần lên lịch.
- **2026-07-18 (module COURSE + COURSE CONTENT — theo yêu cầu tester "test tiếp COURSE và COURSE CONTENT"):**
  - Khảo sát lại từ code thật: phần lớn business logic của CRS+CNT nằm trong `TrainerDashboardServiceImpl` (509 dòng, 12 method dùng chung — đã bị dời 2 lần trước đó: từ đợt EXAM vì là "God class" chung với CRS/CNT, rồi lại không đụng tới ở đợt LEARNING). Lần này làm dứt điểm toàn bộ class trong 1 file test, đóng luôn phần đã deferred từ đợt EXAM (6 method exam-authoring: `getTrainerExams`, `createTrainerExam`, `saveExamQuestions`, `getExamQuestions`, `updateExamStatus`, `deleteTrainerExam`).
  - Viết mới **50 test** trải trên 3 file:
    - `TrainerDashboardServiceImplTest.java` (43 test) — toàn bộ 12 method: `getTrainerDashboardSummary`, `getTrainerCourses` (status counts, search, time-period filter, 3 kiểu sort), `createTrainerCourse` (kèm normalize categoryKey/difficultyKey), `getSystemParametersByType`, `updateTrainerCourse` (section/lesson diffing — thêm/xóa/cập nhật), `publishTrainerCourse`, và 6 method exam-authoring nói trên.
    - Bổ sung 6 test vào `LessonServiceTest.java` cho `getLessonDetail` (dời từ đợt LEARNING vì tưởng JdbcTemplate khó mock — **hóa ra đơn giản**: `jdbcTemplate.query(...)` tự nó là mock, nên RowMapper/SQL thật không bao giờ chạy, chỉ cần stub thẳng kết quả trả về).
    - `CourseManagerDashboardServiceTest.java` (1 test) — class mới phát hiện (`CourseManagerDashboardServiceImpl`, 33 dòng, chỉ tổng hợp số liệu dashboard, KHÔNG phải luồng review/approve/reject Course — row 35 vẫn là Gap thật).
  - **Lỗi Mockito gặp phải khi viết `TrainerDashboardServiceImplTest`:** gọi `when(mockA.method())` lồng bên trong argument list của `when(mockB.method(...))` (ví dụ `when(courseRepository.find(...)).thenReturn(List.of(detailProjection(...), detailProjection(...)))` với `detailProjection()` tự gọi `when()` bên trong) gây `UnfinishedStubbingException` — Mockito không xử lý được `when()` lồng nhau vì cơ chế ghi nhận stubbing là global/stateful. Sửa bằng cách tách các projection ra biến local trước khi đưa vào `thenReturn()`. Cũng gặp `UnnecessaryStubbingException` ở test lọc theo thời gian (projection bị lọc bỏ trước khi field của nó được đọc) — sửa bằng `Mockito.lenient()` cho helper dùng chung.
  - **2 phát hiện thật, chưa sửa (ghi nhận cho dev):**
    - **Xác nhận dứt điểm** (trước đây chỉ "Verify"): `updateTrainerCourse` không có logic `course_versions`/clone-khi-sửa-course-đã-Published — sửa course ở bất kỳ trạng thái nào (kể cả đã PUBLISHED) đều ghi đè trực tiếp lên cùng 1 row + sections/lessons. BR-CRS-03/§9.7 ("sửa nội dung đã Published → duyệt lại + versioning") chưa được cài đặt, không chỉ là "chưa xác nhận".
    - **`updateExamStatus` không phân biệt vai trò gọi**: bất kỳ Trainer nào sở hữu Exam đều tự set status tuỳ ý (kể cả tự publish), không có gate duyệt của Course Manager như `publishTrainerCourse` có (`TrainerProfile.status=='VERIFIED'`). Không rõ đây có phải cố ý theo FR-EXM-03 (chỉ Exam do Course Manager tạo mới được self-publish) hay là thiếu code — cần hỏi dev.
  - **Phát hiện tính năng mới (code-first):** `CourseImportService.java` (737 dòng, method `importWorkbook`) — đây chính là tính năng "importSyllabusFromExcel" từng bị đánh dấu Planned/Gap ở khảo sát 2026-07-13 (row 41), nay đã có code thật (không dùng Apache POI, tự parse XML thô bên trong file `.xlsx`). **Chưa test đợt này** — quá lớn/phức tạp so với phần còn lại, để dành làm 1 đợt CNT chuyên sâu riêng.
  - Sau khi thêm: `mvnw clean test` → **276/276 pass, 0 skip**.
  - Đã tạo `doc/specs/utc-sheet-course.csv` (51 dòng, định dạng flat).
- **2026-07-19 (module QUESTION BANK — theo yêu cầu tester "test tiếp QUESTION BANK"):**
  - Khảo sát code thật: `TrainerQuestionServiceImpl` (424 dòng, 5 method: `getTrainerQuestions`, `createQuestionBankGroup`, `getQuestionDetail`, `updateQuestionBankGroup`, `updateQuestionStatus`) + `TrainerQuestionAIService` (132 dòng, 1 method: `generatePayload`). Chưa có test nào trước đó (chỉ có `SectionQuestionControllerTest.java` — test Controller của teammate cho 1 class khác, không đụng tới).
  - Viết mới **44 test** trên 2 file: `TrainerQuestionServiceTest.java` (34 test) và `TrainerQuestionAIServiceTest.java` (10 test).
  - **Vấn đề Mockito khi test `getTrainerQuestions`** (build SQL động bằng tay + `JdbcTemplate.query(sql, rowMapper, Object... args)`): stub `any()` cho tham số varargs cuối bị lỗi `PotentialStubbingProblem` vì Mockito không khớp `any()` với `Object[]` thật được truyền vào (code gọi `jdbcTemplate.query(sql, mapper, params.toArray())` — về mặt bytecode, `params.toArray()` là 1 mảng, không phải nhiều đối số rời). Sửa bằng `any(Object[].class)` thay vì `any()` trần. Để test logic mapping của RowMapper (không tự chạy vì JdbcTemplate đã bị mock), dùng `ArgumentCaptor<RowMapper>` bắt lại lambda rồi tự gọi `mapRow()` với `ResultSet` giả.
  - **1 phát hiện thật (GAP-QB-01):** `TrainerQuestionAIService.generatePayload` tính toán 2 biến fallback `categoryId`/`difficultyId` (mặc định 1L/14L khi request không cung cấp) nhưng **không bao giờ dùng tới** — `buildUserInput(req)` đọc thẳng `req.getCategoryId()`/`req.getDifficultyId()` (giá trị null gốc), không dùng biến fallback đã tính. Hệ quả: khi Trainer không chọn category/difficulty lúc nhờ AI sinh câu hỏi, prompt gửi cho Gemini ghi thẳng "CATEGORY_ID(DEFAULT): null" thay vì giá trị mặc định thật. Trông giống tính năng làm dở/quên nối dây, không phải cố ý — chưa sửa, chỉ viết test xác nhận hành vi thật.
  - Sau khi thêm: `mvnw clean test` → **320/320 pass, 0 skip**.
  - Đã tạo `doc/specs/utc-sheet-question-bank.csv` (45 dòng, định dạng flat).
- **2026-07-19 (module AI ASSISTANT — theo yêu cầu tester "test tiếp AI ASSISTANT"):**
  - Khảo sát code thật: `AIAssistantService` (288 dòng, điều phối 3 lớp guardrail), `ScopeGuardrailService` (93 dòng, lớp 3 — embedding similarity), `AIPromptBuilder` (84 dòng, lớp 1 — prompt engineering, pure string), `LessonEmbeddingService` (43 dòng, cache embedding), `GeminiClientService` (205 dòng, wrapper gọi Gemini API thật qua WebClient). Cũng phát hiện `VectorUtil` (util toán học cho cosine similarity) chưa từng được liệt kê trong §1 dù là nền tảng của toàn bộ quyết định guardrail lớp 3.
  - Viết mới **39 test** trên 5 file: `VectorUtilTest.java` (6), `AIPromptBuilderTest.java` (6, không cần mock — pure string building), `ScopeGuardrailServiceTest.java` (7), `LessonEmbeddingServiceTest.java` (4), `AIAssistantServiceTest.java` (16 — file phức tạp nhất module này, đủ 3 lớp guardrail + lịch sử chat + suggestedQuestions, nhưng pass ngay lần chạy đầu tiên).
  - **Quyết định phạm vi:** không viết test cho `GeminiClientService` — class này tự dựng `WebClient` bên trong qua `@PostConstruct`, không có điểm nối để inject mock, và mọi method đều là 1 chuỗi gọi HTTP reactive thật (`WebClient.post().retrieve().bodyToMono().retryWhen().timeout().block()`) tới Gemini API sống. Đây chính là "ranh giới mock" đã ghi trong §1 hàng 60 từ trước — mọi class khác trong module AI đều nhận `GeminiClientService` qua constructor injection và mock nó trực tiếp (đúng như 5 file test đợt này đã làm). Test `GeminiClientService` thật sự cần kiểu WireMock/MockWebServer (integration-style, khác hẳn phạm vi unit test hiện tại).
  - **Xác nhận dứt điểm** hàng 60b (trước đây "Not found"): đọc toàn bộ `sendMessage` xác nhận KHÔNG có bước RAG-style chunk-retrieval nào — toàn bộ "context" đưa cho AI chỉ là nội dung Lesson (content text) + practice questions, nhúng thẳng dạng text vào prompt/scope-check, không có vector-store lookup hay chunking.
  - Không phát hiện gap/bug mới ở module này — 3 lớp guardrail (embedding similarity, prompt engineering, backend context check) hoạt động đúng như comment trong code mô tả, kể cả các nhánh fail-open có chủ đích (embedding lỗi → vẫn cho qua để Gemini tự chối khéo; suggestedQuestions lỗi → vẫn trả lời chính vẫn thành công).
  - Sau khi thêm: `mvnw clean test` → **359/359 pass, 0 skip**.
  - Đã tạo `doc/specs/utc-sheet-ai-assistant.csv` (40 dòng, định dạng flat).
- **2026-07-22 (module COMMENT MANAGEMENT — theo yêu cầu "design + implement" Comment Management, sau đó viết test):**
  - Module này đã có code **một phần** từ trước (`Comment` entity, `CommentService`/`CommentServiceImpl`, `CommentController`, `AdminCommentController`, `comment_management_page.dart` ở Flutter) nhưng theo đúng như khảo sát ở đợt 2026-07-19 (`qa-report-for-dev-team.md`): "COMMENT MANAGEMENT... chưa test". Yêu cầu nghiệp vụ mới: Rule Engine đơn giản (không AI) — normalize text (lowercase, bỏ dấu, gộp khoảng trắng, chuẩn hoá ký tự đặc biệt) → check blacklist keyword + regex pattern → gán status `APPROVED`/`PENDING`/`REJECTED`.
  - Viết mới `CommentRuleEngineService` (normalize + blacklist + regex, không có Service khác implement chung interface nên để class cụ thể, không tạo interface thừa) + `CommentModerationResult` (record). Sửa `CommentServiceImpl.addComment`/`updateComment` để chạy qua Rule Engine thay vì hardcode `status="APPROVED"` như trước (**gap thật, đã sửa**: code cũ hoàn toàn không có bước duyệt nào). Sửa `getCommentsByLesson` để ẩn `PENDING`/`REJECTED` khỏi người xem khác (trước đây trả về **toàn bộ** comment bất kể status — cũng là gap thật).
  - **2 bug thật tìm thấy và sửa ngay trong đợt, do tester phát hiện qua test tay (không phải do unit test tìm ra trước):**
    1. **Blacklist quá sơ sài lúc đầu** — bản đầu chỉ có vài từ placeholder (`dm`, `vcl`, `fuck`, `shit`...), thiếu gần hết từ tục tiếng Việt phổ biến nên hầu hết comment "bậy bạ" thật vẫn lọt qua thành `APPROVED`. Đã mở rộng danh sách (`dm/dmm/dcm/dkm/vcl/vloz/clgt`, `dit me/dit con me/du me`, `lon/buoi/deo/suc vat/oc cho`, tục tiếng Anh...) và thêm tầng `PENDING` mới cho các từ mỉa mai nhẹ (`ngu/dien/khung/stupid/idiot`...). **Giới hạn cố hữu đã ghi nhận, không sửa được bằng cách thêm từ:** bỏ dấu gây trùng thật — `cặc` (tục) và `các` (từ cực kỳ thông dụng "các bạn") đều normalize thành `cac`, nên **cố tình không đưa `cac` vào blacklist** để tránh chặn nhầm phần lớn comment bình thường (xem `doc/specs/13-comment-management.md` §5).
    2. **`isVisibleTo` cho tác giả thấy lại cả comment `REJECTED` của chính mình** (chỉ nên áp dụng cho `PENDING`) — khiến comment vi phạm đã bị Rule Engine gắn cờ `REJECTED` vẫn hiển thị lại trong khung thảo luận của chính tác giả, tester test tay phát hiện ngay ("phát hiện rồi mà comment vẫn hiện ở đó"). Đã sửa: `REJECTED` ẩn tuyệt đối kể cả với tác giả; `PENDING` vẫn cho tác giả thấy (để biết đang chờ duyệt).
  - Viết `CommentRuleEngineServiceTest.java` (10 test) + `CommentServiceImplTest.java` (19 test) — tổng **29 test mới**, tất cả pass.
  - **Quyết định phạm vi:** không viết test cho `CommentController`/`AdminCommentController` (Controller-layer, theo đúng chính sách Service-only đã chốt từ 2026-07-17).
  - Sau khi thêm: `mvnw clean test` → **388/388 pass, 0 skip**.
  - Đã tạo `doc/specs/utc-sheet-comment.csv` (29 dòng, định dạng flat).
- **2026-07-22 (module COURSE RATING & REVIEW — mở rộng `CourseRatingServiceImpl` theo spec mới, "chỉ Course Manager" quản lý rating theo quyết định của tester khi được hỏi Admin/Course Manager/cả hai):**
  - Khảo sát code thật trước khi sửa: `CourseRatingServiceImpl.addCourseReview` **chưa có** eligibility check nào (không kiểm tra enrolled/completed), `CourseReviewRequestDTO.content` đang `@NotBlank` (bắt buộc, trái spec "review optional"), `Course` entity chưa có field cache rating, và **toàn bộ module Notification chưa có code gì** (đúng như dòng 84-85 §1 đã ghi "Planned").
  - **Phát hiện quan trọng làm đổi hướng thiết kế:** đọc `review_tab.dart` (frontend) thấy đã có sẵn luồng **Edit Review** hoàn chỉnh (PopupMenuButton Edit/Delete cho review của chính mình) — nên diễn giải "have not submitted a rating before" trong spec mới là điều kiện cho lần đăng **đầu tiên**, KHÔNG phải lệnh cấm sửa lại; giữ nguyên hành vi upsert cũ (1 slot/learner/course, editable) thay vì biến thành lỗi cứng — tránh phá tính năng Edit đang chạy tốt.
  - Thêm eligibility gate thật vào `addCourseReview`: bắt buộc có `Enrollment` (đã enroll) và `status='COMPLETED'`, dùng `EnrollmentRepository.findByUserIdAndCourseId` có sẵn. Bỏ `@NotBlank` khỏi `CourseReviewRequestDTO.content`.
  - Thêm `Course.averageRating`/`totalRatings` (cache, tính lại mỗi lần add/update/delete rating qua `recalculateCourseStats`). Thay đoạn tính live-từ-DB trong `CourseServiceImpl.getCourseDetail` bằng đọc thẳng 2 field cache này (bỏ luôn `CourseRatingRepository` dependency khỏi `CourseServiceImpl` — không còn dùng tới), thêm `totalRatings` vào `CourseDetailDTO`.
  - Vì **Notification module hoàn toàn chưa có code** (đã kiểm tra `find -iname "*notif*"` không ra kết quả nào), tạo mới tối giản: `Notification` entity + `NotificationRepository` + `NotificationService` (chỉ lưu DB, không realtime/email — xem row 84b ở §1). `addCourseReview` gọi `notifyCourseManagers` khi rating ≤3 sao (FR-LRN-10), và khi average chuyển từ >4.0 xuống ≤4.0 (FR-LRN-11, chỉ bắn lúc "vượt ngưỡng", so với giá trị cache **trước khi** lưu rating mới, không lặp lại ở các rating thấp tiếp theo và không bắn ở lượt rating đầu tiên vì chưa có baseline để so sánh).
  - **Xác nhận role thật cho "Course Manager"** trước khi gate 2 endpoint mới: `CourseManagerDashboardController` dùng `hasRole('TRAINER_LEAD')`, không phải `'COURSE_MANAGER'` (chuỗi đó chỉ xuất hiện lẻ tẻ 1 lần ở `TrainerExamMatrixController`, nhiều khả năng là chỗ code cũ dùng sai/không nhất quán — **flag lại cho dev, không tự sửa** vì ngoài phạm vi đợt này). Thêm `GET/PUT /api/v1/course-manager/notifications` vào `CourseManagerDashboardController` (không tạo Controller mới), cùng gate `hasRole('TRAINER_LEAD')`.
  - Viết mới `NotificationServiceTest.java` (1 test) + mở rộng `CourseRatingServiceTest.java` từ 11 lên **21 test** (10 test mới: eligibility x2, blank-content x1, cache-recalc x1, low-rating-notify x2, average-crossing-notify x4). Sửa 4 test cũ trong `CourseServiceTest.java` (getCourseDetail nhóm) bị vỡ do đổi cách tính rating từ live-recompute sang đọc cache — không phải bug, chỉ là test cũ giả định sai cách triển khai mới.
  - Sau khi thêm: `mvnw clean test` → **399/399 pass, 0 skip**.
  - Đã tạo `doc/specs/utc-sheet-notification.csv` (1 dòng) và cập nhật `doc/specs/utc-sheet-learning.csv` (thêm/sửa các dòng `CourseRatingServiceImpl`/`getCourseDetail` liên quan, xem note cuối file đó).
  - **Chưa làm, ghi nhận cho đợt sau nếu cần:** chưa có màn hình UI cho Course Manager xem danh sách notification (mới có API `GET/PUT .../notifications`, chưa có trang Flutter tương ứng — `presentation/pages/course_manager/` hiện chưa có file notification nào).
- **2026-07-22 (module ROLE & PERMISSION MANAGEMENT — theo yêu cầu tester "hoàn thiện đầy đủ và chỉn chu", tự động viết test sau khi làm xong):**
  - Khảo sát toàn bộ module qua subagent trước khi sửa (tránh vừa dò vừa code): `AdminController` (315 dòng, không có Service riêng — đúng như báo cáo QA 2026-07-19), `Role` entity chỉ có `{id, roleName}` (không có cột permission nào), `user_roles` join table, **không có `Permission`/`RolePermission` entity, không có `AuditLog`, không có bảng AI-usage nào** trong toàn bộ codebase. Frontend: toàn bộ RBAC UI nằm chung 1 file `admin_dashboard_page.dart` (4684 dòng) — tab Dashboard (chỉ user+role count, không courses/enrollments/revenue), tab Roles (4 card tĩnh, text hardcode kiểu marketing, không gọi API), tab AI Analytics (100% số liệu giả hardcode, ví dụ '24,805 Total AI Consults').
  - **Hỏi tester 1 quyết định kiến trúc thật sự cần input** (không tự quyết vì rủi ro lớn): FR-RBAC-05 (Permission Management) — xây permission matrix động thật (đổi toàn bộ `@PreAuthorize("hasRole")` sang `hasAuthority()` đọc từ DB, hàng chục controller) hay giữ static/read-only? Tester chọn **giữ static, chỉ làm cho chính xác** — không refactor kiến trúc phân quyền.
  - Hỏi thêm về phạm vi Audit Log (FR-RBAC-08) và AI Usage Monitoring (FR-RBAC-07, cả 2 đều hoàn toàn chưa có code) — tester giao lại quyết định ("bạn nghĩ hướng nào là tốt nhất"), chọn xây **cả 2**, ở mức tối giản/thật (không fake số liệu), theo đúng đề xuất Recommended đã đưa ra.
  - **3 bug thật tìm thấy và sửa (không phải do test tìm ra trước — phát hiện khi đọc code để sửa "cho chỉn chu"):**
    1. **`updateUserByAdmin` bypass hoàn toàn whitelist status + không có self-lock check** — nghiêm trọng nhất: endpoint này set `user.setStatus(...)` trực tiếp không qua `ALLOWED_USER_STATUSES` như `updateUserStatus`, và Admin có thể tự khoá tài khoản chính mình qua đường này dù `updateUserStatus` đã chặn. Đã đồng bộ cả 2 endpoint dùng chung 1 whitelist + self-lock check.
    2. **`AuthService.createUserByAdmin` không whitelist role** — chấp nhận bất kỳ chuỗi non-empty nào, nếu role chưa tồn tại thì **âm thầm tạo mới** 1 row rác trong bảng `roles` (gõ nhầm chính tả cũng tạo role mới vĩnh viễn). Đã thêm whitelist `LEARNER`/`TRAINER`/`TRAINER_LEAD`/`ADMINISTRATOR`.
    3. **`getDashboardStats` tự bịa số liệu biểu đồ tuần** — khi không có đăng ký mới trong 7 ngày, code tự tính `totalUsers/7` rồi rải vào các ngày, không phân biệt được với dữ liệu thật. Đã xoá logic này, tuần không có đăng ký giờ hiện đúng toàn số 0.
  - Xây mới, tối giản, dựa trên dữ liệu thật: `AuditLog` entity + repository (log `CREATE_USER`/`UPDATE_USER`/`UPDATE_USER_STATUS`, gọi trực tiếp trong `AdminController`, không tạo Service mới — giữ đúng convention "logic nằm thẳng trong Controller" của class này); `AiUsageLog` entity + repository, gắn instrumentation vào `GeminiClientService.generateChatResponse`/`generateEmbedding` (điểm chốt duy nhất mọi lời gọi Gemini thật đi qua — không cần sửa AIAssistantService/TrainerQuestionAIService/ExamCourseRecommendationAIService riêng lẻ). Thêm `GET /api/admin/audit-log`, `GET /api/admin/ai-usage`, mở rộng `GET /api/admin/dashboard/stats` (totalCourses/totalEnrollments/topCourses qua 1 native query mới `CourseRepository.findTopCoursesByEnrollment`).
  - Frontend: sửa tab Roles (mô tả quyền hạn đúng thực tế code, đổi tên hiển thị "TRAINER LEAD" → "COURSE MANAGER (role: TRAINER_LEAD)" cho rõ, ghi chú thẳng "Course review/approve workflow chưa tồn tại" thay vì giả vờ đã có); nối tab AI Analytics vào `/admin/ai-usage` thật (bỏ số liệu giả, bỏ luôn danh sách "Recent AI Interactions" fabricated vì `AiUsageLog` không lưu chi tiết ngữ nghĩa từng lượt gọi — chỉ hiện breakdown Chat/Embedding thật); thêm tab mới "Audit Log" (tái dùng `_buildInteractionRow` sẵn có thay vì viết widget mới); mở rộng tab Dashboard (4 summary card thay vì 2, thêm bảng Top Courses by Enrollment).
  - Viết lại hoàn toàn `AdminControllerTest.java` (4 → 25 test, phủ hết `getUsers`/`getUserDetail`/`createUserByAdmin`/`updateUserByAdmin`/`getDashboardStats`/`getAuditLog`/`getAiUsageStats`) + thêm 6 test mới vào `AuthServiceTest.java` cho `createUserByAdmin` (trước đó **chưa có test nào**, đúng như báo cáo QA 2026-07-19). Lần chạy đầu tiên `updateUserStatusShouldUpdateStatusForAnotherUser` (test cũ) fail 400 thay vì 200 — nguyên nhân: thêm field `auditLogRepository` mới vào `AdminController` nhưng test cũ dùng `@InjectMocks` không có mock tương ứng → gọi `.save()` trên field null → NPE bị catch chung thành lỗi 400. Sửa bằng cách thêm đủ `@Mock` cho `AuditLogRepository`/`AiUsageLogRepository`/`CourseRepository`/`EnrollmentRepository` vào test class.
  - **Quyết định phạm vi:** không viết test cho `GeminiClientService` (kể cả sau khi thêm instrumentation) — vẫn là "ranh giới mock" đã ghi nhận từ đợt AI ASSISTANT (2026-07-19), lý do không đổi (tự dựng WebClient thật qua `@PostConstruct`, không có điểm nối). Không đụng vào `TrainerExamMatrixController`'s `'COURSE_MANAGER'` string lẻ loi (bug có thật, nhưng sửa role-check ở module khác ngoài phạm vi RBAC đợt này) — chỉ flag lại cho dev.
  - Sau khi thêm: `mvnw clean test` → **426/426 pass, 0 skip**.
  - Đã tạo `doc/specs/utc-sheet-rbac.csv` (31 dòng, định dạng flat) và viết lại `doc/specs/03-rbac.md` cho khớp implementation thật (endpoint path, role name `ADMINISTRATOR` không phải `ADMIN`, bỏ mô tả sai `fl_chart`/`@Scheduled` job chưa từng tồn tại).
- **2026-07-24 (rà soát toàn dự án theo yêu cầu tester "kiểm tra đã phủ hết feature chưa, hoàn thiện Notification, test Payment & Revenue và Notification, test nốt phần còn thiếu"):**
  - **Merge `origin/dev` → `unit-test` trước khi làm** (53 commit) — phát sinh 25 test lỗi/vỡ do code production đổi mà test cũ chưa cập nhật (đúng kịch bản "final retest" đã ghi nhận trước đó): `CourseManagerDashboardServiceTest` (đổi tên method repository), `CourseServiceTest` (`getCourseDetail` đổi sang `findByIdWithDetails`, `enrollCourse` đổi thứ tự check), `TrainerDashboardServiceImplTest` (thêm `PaymentRepository`/`CourseRatingRepository`, bug Mockito mock boxed `Long` trả về `0L` thay vì `null` làm gộp nhầm version-group), `ExamServiceTest` (viết lại gần như toàn bộ — `saveExamAttempt` đổi sang luôn tính điểm server-side từ đáp án đúng, bỏ hẳn việc tin điểm client gửi lên; learner-count đổi từ chuỗi "1k Learner" sang số thô; `getExamAttempts`/`getMyExamAttempts` đổi sang đếm theo `startedAt` thay vì `submittedAt`). Đã sửa hết, không đổi code production nào trong bước này (chỉ sửa test cho khớp code thật).
  - **Khảo sát feature coverage toàn dự án** qua subagent: xác nhận hầu hết module đã đủ Controller+Service; 2 module yếu nhất đúng theo nghi vấn ban đầu của tester — Notification (chỉ 1 method stub, role-broadcast, không có user_id) và Payment & Revenue (code đầy đủ — Cart/Payment/MonthlyStatement — nhưng gần như 0% test thật: `PaymentServiceTest.java` cũ test nhầm code VNPay đã bị thay bằng PayOS).
  - **Hỏi tester 3 quyết định phạm vi trước khi code:** (1) mở rộng Notification sang per-user + thêm trigger sự kiện (chọn, thay vì chỉ hoàn thiện trong khuôn khổ role-broadcast cũ); (2) sửa lỗ hổng `@PreAuthorize("permitAll()")` ở 3 endpoint course-manager của `MonthlyStatementController` (chọn sửa luôn); (3) thay thế hoàn toàn `PaymentServiceTest.java` (VNPay chết) bằng test PayOS thật thay vì giữ song song (chọn thay thế, xoá file cũ).
  - **Notification — mở rộng thành module per-user thật:** thêm `Notification.user` (nullable, per-user target) song song `recipientRole` cũ (nullable, role-broadcast, giữ nguyên hành vi cho `notifyCourseManagers`); `NotificationRepository` thêm `findVisibleToUser`/`countUnreadVisibleToUser`/`markAllAsReadForUser` (khớp cả 2 kiểu targeting bằng 1 query); `NotificationService` thêm `notifyUser`/`getNotificationsForUser`/`getUnreadCount`/`markAsRead`(có ownership check)/`markAllAsRead`; `NotificationController` mới (`/api/v1/notifications/**`) dùng chung cho mọi role, không thay thế API cũ của `CourseManagerDashboardController` (vẫn chạy song song). Nối **7 trigger** vào đúng nghiệp vụ đang có sẵn (không thêm entity/luồng mới): `PurchaseSuccess`+`NewEnrollment` trong `PaymentServiceImpl.handlePayOSWebhook`, `NewEnrollment` trong `CourseServiceImpl.enrollCourse` (khoá học miễn phí), `CommentReply` trong `CommentServiceImpl.addComment` (bỏ qua tự-trả-lời chính mình), `ContentApproved`/`ContentRejected` trong 4 method publish/return của `CourseManagerDashboardServiceImpl`, `CourseUpdated` khi publish version mới (báo cho toàn bộ learner đang enroll ở version cũ, dùng `EnrollmentRepository.findByCourseIdIn` mới thêm), `StatementReady` trong `MonthlyStatementServiceImpl.generateMonthlyCutoff`. Không làm WebSocket/STOMP realtime và không chuyển sang `ApplicationEventPublisher` — nằm ngoài phạm vi đã thống nhất, ghi lại là Planned trong `14-notification.md` §3.
  - **Frontend:** nối chuông thông báo thật ở `shared_header.dart` — thêm `NotificationRepository`/`NotificationItem`, badge đỏ đếm unread, danh sách thật trong dropdown (tái dùng `_buildNotificationItem` có sẵn nhưng chưa từng được gọi), bấm vào 1 thông báo → gọi mark-as-read thật, nút "Đánh dấu đã đọc" cho mark-all. Trước đó chuông chỉ là UI giả, luôn hiện "No new notifications", không gọi API nào.
  - **Sửa lỗ hổng phân quyền thật (không phải chỉ ghi nhận):** `MonthlyStatementController` — 3 endpoint course-manager (`GET/POST .../statements`, `.../statements/generate`, `.../statements/{id}/settle`) đổi từ `@PreAuthorize("permitAll()")` sang `hasAnyAuthority('TRAINER_LEAD', 'COURSE_MANAGER', 'ADMINISTRATOR', ...)` khớp các endpoint course-manager khác cùng codebase.
  - **Payment & Revenue — xoá `PaymentServiceTest.java` cũ** (test HMAC-SHA512 VNPay tự đứng riêng, không hề gọi tới `PaymentServiceImpl`/`PaymentRepository` nào — coi như 0% coverage thật dù nhìn tên file tưởng có test). Viết mới hoàn toàn dựa trên code PayOS thật: `PaymentServiceImplTest.java` (13 test — chữ ký webhook, idempotency, revenue split 70/30 vs 60/40, auto-enroll, notification, payment status/history), `CartServiceImplTest.java` (15 test, module này trước đó chưa có test nào), `MonthlyStatementServiceImplTest.java` (15 test, cũng chưa từng có test).
  - **Quyết định phạm vi:** không viết test cho `PaymentServiceImpl.createPayment` (tạo link thanh toán PayOS) — gọi thẳng PayOS HTTP API qua `new RestTemplate()` khởi tạo inline trong method private, không phải client có thể inject/mock; muốn test được phải refactor production code (tách client thành field/interface) — ngoài phạm vi đợt test này, ghi nhận là gap trong `utc-sheet-payment.csv`.
  - Sau khi thêm: `mvnw clean test` → **488/488 pass, 0 skip** (32 lớp test).
  - Đã tạo `doc/specs/utc-sheet-payment.csv` (43 dòng, định dạng flat — Payment + Cart + MonthlyStatement) và mở rộng `doc/specs/utc-sheet-notification.csv` (1 → 20 dòng). Viết lại `doc/specs/14-notification.md` (bỏ merge-conflict marker còn sót từ trước, mô tả đúng kiến trúc per-user + role-broadcast song song) và `doc/specs/12-payment-revenue.md` (PayOS thay VNPay, xác nhận entities đã tồn tại, làm rõ payout do Course Manager/Admin qua settlement endpoint chứ không phải "Admin quản lý" như Decision Log cũ ghi).
  - **Chưa làm, ghi nhận cho đợt sau nếu cần:** WebSocket/STOMP realtime notification; chuyển trigger sang `ApplicationEventPublisher`; trang danh sách thông báo riêng (hiện chỉ có dropdown ở chuông); test cho `PaymentServiceImpl.createPayment` (cần refactor production code trước); đối soát lại số Sub-total ở bảng Test Priority Plan §2 (không tự đoán số, xem note tại chỗ).
- **2026-07-24 (tiếp — audit toàn dự án 4 phase: Phase 1 code/architecture audit, Phase 2 test audit, Phase 3 doc sync, Phase 4 test-doc sync, Phase 5 roadmap; yêu cầu tester "chuẩn bị bản ổn định cho documentation và phát triển tiếp"):**
  - Phase 1/2 là **audit thuần** trước, không sửa gì — kết quả đầy đủ nằm ở `doc/AUDIT_REPORT.md` (Critical/High/Medium/Low/Suggested) và `doc/TEST_AUDIT_REPORT.md` (coverage summary, danh sách method thiếu test, ưu tiên đề xuất). Không lặp lại toàn bộ nội dung ở đây — 2 file đó là nguồn chính thức.
  - Từ danh sách ưu tiên trong `TEST_AUDIT_REPORT.md`, đã bổ sung có chọn lọc (không test tràn lan) 5 method/class có rủi ro nghiệp vụ cao nhất mà trước đó chưa có test: `PaymentExpirationScheduler.expirePendingPayments` (3 test, class mới hoàn toàn — xem hàng 78c ở §1), `ExamCourseRecommendationAIService.recommendCoursesAI` (7 test — hàng 71), `ExamService.getExamQuestions` (5 test — hàng 54b), `CourseManagerDashboardServiceImpl.getCoursesForReview`/`getCourseReviewDetail`/`getExamsForReview` (11 test — hàng 35), `MonthlyStatementServiceImpl.getTrainerStatements` (3 test — hàng 78b). Tổng **+29 test mới**, không sửa hành vi code production nào để test pass (chỉ thêm 1 static import `assertTrue` bị thiếu trong file test cũ).
  - **Quyết định phạm vi (nhất quán với các đợt trước):** không test `CloudinaryService`/`EmailService` (wrapper mỏng, đã mock gián tiếp qua các test khác), không test `GeminiClientService` (không có seam để mock, đã ghi nhận từ 2026-07-19), không test `CourseImportService` (737 dòng, đã hoãn có chủ đích từ 2026-07-18), không test `CourseServiceImpl.switchCourseVersion`/`getCourseVersionHistory` hay `TrainerDashboardServiceImpl.approveTrainerCourse`/`rejectTrainerCourseDraft` (5+2 method còn thiếu — để đợt sau, 2 method sau còn phụ thuộc quyết định sản phẩm về HIGH-04 trước khi viết test khoá 1 phía tuỳ ý của sự không nhất quán).
  - Sau khi thêm: `mvnw clean test` → **517/517 pass, 0 skip, 0 fail** (34 lớp test, 32 cũ + 2 mới: `PaymentExpirationSchedulerTest`, `ExamCourseRecommendationAIServiceTest`).
  - Phase 3: đồng bộ lại `HanGo_Documentation.md` (thêm §15-23: package structure, DB overview, API overview, sequence diagram, deployment, configuration, testing strategy, known limitations, future roadmap — giữ nguyên toàn bộ §1-14 cũ, chỉ sửa các claim sai cụ thể: PayOS thay VNPay, giá trị enum thật). Đồng bộ `README.md`/`ARCHITECTURE.md`/`CONSTITUTION.md`/`AGENTS.md`/`agent_backend.md`/`agent_frontend.md`/`agent_qa.md` (Java 17 không phải 21, không dùng MapStruct/Riverpod/go_router thật, PayOS, chưa có migration tool). Cập nhật `doc/specs/05/06/08` (contradiction rõ ràng — versioning, Apache POI, GAP-EXM-01) và ghi chú nhẹ ở `01/02/04/07/09/10/11`.
  - Phát hiện nghiêm trọng ngoài phạm vi test (đã báo tester trực tiếp, xem `AUDIT_REPORT.md` CRIT-01..04): `TestDBController` không xác thực + chạy SQL phá hoại kể cả reset mật khẩu; `CommentController`/`LessonController` nhận `userId` từ client thay vì SecurityContext; `SectionQuestionController` thiếu `@PreAuthorize`; `DEPLOY_GUIDE.md` từng chứa giá trị thật của PayOS credentials (đã redact giá trị trong file, **chưa** rotate key thật — cần tester tự làm trên PayOS Dashboard). Không tự sửa các lỗ hổng này (thay đổi API contract/hành vi cần xác nhận theo `AGENTS.md` §10) — chỉ redact secret trong doc vì đó là thay đổi tài liệu thuần.
  - Phase 5: tạo `doc/ROADMAP.md` (kế hoạch version sau, có ưu tiên) theo yêu cầu tester "đóng version 1.0, nêu những thứ cần cải tiến cho version sau".
  - **Chưa làm, cần tester quyết định:** xoá các file rác đã xác nhận chết (`_debug_provider_check.dart`, `test.dart`/`test_uri.dart`/`analyze_output.txt` ở `hango-frontend`, `CheckDBApp.java` trong test tree, `response.json` mồ côi, thư mục `test/` rỗng ở gốc repo) — bị chặn bởi permission classifier của phiên làm việc, cần tester xác nhận hoặc tự xoá thủ công.

---

## 3. Detailed Unit Test Case (UTC) Designs

Mẫu trình bày mỗi UTC gồm 2 phần: (a) **header block** (Module/Method/Created By/Executed By/Test requirement/Passed-Failed-Untested/N-A-B/Total), (b) **ma trận Condition/Confirm/Result** theo UTCID (tham khảo định dạng do tester cung cấp). `Created By`/`Executed By` để trống cho tester điền khi phân công thật.

### 3.1 UTC design for: `AuthService.authenticateUser`

> **Implemented** — [`AuthServiceTest.java`](../../hango-backend/src/test/java/com/hango/hango_backend/service/AuthServiceTest.java) (6 tests, all green as of 2026-07-14, `mvnw test -Dtest=AuthServiceTest`). Original design had 10 UTCID slots; 4 were dropped because they test `@Valid` DTO constraints (null/empty/255-char email) which Mockito never sees — those belong to a future MockMvc/`@WebMvcTest` layer, not this Service-level suite (see class javadoc for the rationale).

| Code Module | Authentication | Method | `authenticateUser` |
|---|---|---|---|
| Created By | tester | Executed By | Claude (pair-tested with tester) |
| Test requirement | Authenticate user credentials, check account status, record login timestamp, issue JWT session. | | |
| Passed / Failed / Untested | 6 / 0 / 0 | N / A / B | 1 / 3 / 2 |

```
UTC ID:                    UTCID01              UTCID02              UTCID03              UTCID04              UTCID05                   UTCID06
---------------------------------------------------------------------------------------------------------------------------------------------------
Test method (AuthServiceTest):
                    authenticateUser     ...BadCredentials     ...RejectInactive   ...RejectInactive    ...AllowLoginForNon        ...ThrowUsernameNotFound
                    ShouldReturnToken    ExceptionOnWrong      Account             CaseInsensitively    InactiveStatusSuchAsLocked WhenUserMissingAfterAuth
                    AndUpdateLastLogin   Password
---------------------------------------------------------------------------------------------------------------------------------------------------
Inputs:
 - Email            active@example.com  active@example.com  inactive@example.com inactive2@example.com locked@example.com        ghost@example.com
 - Password          correct             wrong                correct              correct              correct                    any
 - User.status       ACTIVE              ACTIVE               INACTIVE             "inactive" (lowercase) "LOCKED"*                 ACTIVE (user record missing)
---------------------------------------------------------------------------------------------------------------------------------------------------
Confirm / Expected Result:
 - Exp Return        LoginResponse       Error                Error                Error                LoginResponse**            Error
 - Exception         None                BadCredentialsEx     IllegalArgumentEx    IllegalArgumentEx    None**                     UsernameNotFoundEx
 - Save DB?          Yes (lastLoginAt)   No                   No                   No                   Yes**                      No
---------------------------------------------------------------------------------------------------------------------------------------------------
Case Type:           N                   A                    A                    B                    B                          A
Status:              Passed              Passed               Passed               Passed               Passed                     Passed
Exec Date:           2026-07-14          2026-07-14           2026-07-14           2026-07-14            2026-07-14                 2026-07-14
Defect ID:           —                   —                    —                    —                    —                          —
```

*`UTCID05` is a deliberate boundary/gap probe, not a spec assumption: current code (`AuthService.authenticateUser`) only rejects `status.equalsIgnoreCase("INACTIVE")`. A user with `status = "LOCKED"` (or any other non-`"INACTIVE"` string) **logs in successfully** — confirmed by running the test against real code. If the team intends `LOCKED` to block login (per `doc/specs/01-authentication.md` §4 lockout edge case), this test should start failing once the guard is added — update it then, don't soften it now to hide the gap.
**`googleLogin`'s equivalent risk is documented separately in §3.5-note; `refreshToken`/`logout` do not exist at all — see `unit_test_plan.md` §1 row 10b.

---

### 3.2 UTC design for: `AuthService.registerUser`

> **Implemented** — [`AuthServiceTest.java`](../../hango-backend/src/test/java/com/hango/hango_backend/service/AuthServiceTest.java) (7 tests, all green as of 2026-07-16). Dropped the `@Valid`-only boundary rows (invalid email format, short/null password, 255-char email) for the same reason as §3.1 — Mockito never runs Bean Validation, so those assert nothing at Service level.
>
> **⚠️ Behaviour changed 2026-07-16 (upstream commit `92034ec` on `dev`, by a teammate, unrelated to this test work):** `registerUser` used to hardcode role="LEARNER" regardless of request content (confirmed by the original UTCID04 below, now rewritten). It now reads `RegisterRequest.getRole()` and accepts **either `"LEARNER"` or `"TRAINER"`**, rejecting anything else with `IllegalArgumentException("Error: Invalid registration role!")`. Self-registering directly as `"TRAINER"` via `POST /api/auth/register` skips the Trainer Onboarding application steps (FR-TRN-01..03: type selection, bank info, document upload) — no `TrainerProfile` row exists at registration time. **Revised 2026-07-17, no longer treated as an unreviewed regression:** traced the actual revenue gate — `TrainerDashboardServiceImpl.publishTrainerCourse` still blocks publishing/selling any Course unless `TrainerProfile.status='VERIFIED'` (Admin-reviewed), and the profile row is JIT-created as `PENDING_VERIFICATION` the first time the account touches `GET /trainers/profile`. So this grants early Trainer-role/dashboard access, not a bypass of Admin approval for monetization. `HanGo_Documentation.md` (FR-AUTH-01, BR-TRN-01, §9.2) updated the same day to describe this two-checkpoint reality instead of flagging it as a gap. Tests below match *actual* current behaviour, per this project's code-first testing principle.

| Code Module | Authentication | Method | `registerUser` |
|---|---|---|---|
| Created By | tester | Executed By | Claude (pair-tested with tester) |
| Test requirement | Register a new account, enforce email uniqueness, hash password, assign LEARNER or TRAINER role (whitelist), dispatch verification email. | | |
| Passed / Failed / Untested | 7 / 0 / 0 | N / A / B | 1 / 3 / 3 |

```
UTC ID:              UTCID01                UTCID02                UTCID03                  UTCID04                    UTCID05                     UTCID06                  UTCID07
----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
Test method (AuthServiceTest):
              registerUserShouldCreate  ...ShouldReject       ...ShouldCreateLearner   ...ShouldAllowSelf          ...ShouldRejectRole         ...ShouldStillSucceed
              LearnerAccountAndSend     DuplicateEmail        RoleWhenItDoesNot        RegistrationAsTrainer       OutsideLearnerTrainer       WhenVerificationEmail
              VerificationEmailOnHappy                        ExistYet                 WhenRoleRequested           Whitelist                   DispatchFails
----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
Inputs:
 - Email             new@example.com       existing@example.com   new2@example.com         new3@example.com            new5@example.com            new4@example.com
 - Role field on DTO none                  none                   none                     "TRAINER"                   "ADMINISTRATOR"              none
 - Precondition      "LEARNER" role exists existsByEmail=true     "LEARNER" role NOT found "TRAINER" role exists       existsByEmail=false          emailService throws
----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
Confirm / Expected Result:
 - Exp Return        UserResponse           Error                  UserResponse             UserResponse (roles=[TRAINER]) Error                    UserResponse (no propagation)
 - Exception         None                   IllegalArgumentEx      None                     None                         IllegalArgumentEx: "Invalid registration role!"  None (caught internally)
 - Saved role        LEARNER                N/A                    LEARNER (newly created)  TRAINER                     N/A                          LEARNER
 - Password hashed   Yes                    N/A                    Yes                      Yes                         N/A                          Yes
----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
Case Type:           N                      A                      B                        B                            A                            A
Status:              Passed                 Passed                 Passed                   Passed                       Passed                       Passed
Exec Date:           2026-07-16             2026-07-16             2026-07-16               2026-07-16                   2026-07-16                   2026-07-16
Defect ID:           —                      —                      —                        GAP-AUTH-04                  —                            —
```

*`UTCID04` (renamed from the old "ignore client-supplied role" test): confirms `"TRAINER"` is now a legitimate self-registration choice — flagged as `GAP-AUTH-04`, see the warning above.*
*`UTCID05` (new): confirms the whitelist rejects anything other than `LEARNER`/`TRAINER` (e.g. `"ADMINISTRATOR"`), so at least privilege escalation to Admin is not possible via this path.*
*`UTCID07` documents a real asymmetry: `registerUser` swallows email-dispatch failures (logs and continues), while `resendVerificationEmail` re-throws (see §3.7). Not a bug fix target here — just recorded so the difference in behaviour is intentional-looking, not accidental-looking.*

---

### 3.2b Remaining AUTHENTICATION module methods (consolidated)

> **Implemented** — 2026-07-14, all Passed. Full per-input/per-case detail is in the test source rather than repeated here as an ASCII matrix, to keep this section proportionate; open each file for the exact inputs/assertions.

| Method | Class | Test file | Tests | N / A / B | Key findings |
|---|---|---|:--:|:--:|---|
| `googleLogin` | AuthService | `AuthServiceTest.java` | 1 executed + 1 `@Disabled` | 0/1/0 | Happy-path (JIT-provision) **not unit-testable as written** — `GoogleIdTokenVerifier` is constructed inline via `new`, not injected. Only the malformed-token error path is covered. Recommend extracting an injectable verifier if real unit coverage is wanted. |
| `forgotPassword` | AuthService | `AuthServiceTest.java` | 2 | 1/1/0 | OTP is a 6-digit numeric string, ~5 min expiry — both asserted directly against saved entity. |
| `verifyOtp` | AuthService | `AuthServiceTest.java` | 3 | 1/1/1 | Valid OTP is **not deleted** by `verifyOtp` itself — only by `resetPassword`. Confirmed by asserting `delete()` is never called on the happy path. |
| `resetPassword` | AuthService | `AuthServiceTest.java` | 2 | 1/1/0 | — |
| `verifyAccount` | AuthService | `AuthServiceTest.java` | 2 | 1/1/0 | — |
| `resendVerificationEmail` | AuthService | `AuthServiceTest.java` | 4 | 1/2/1 | **Behavioural asymmetry vs `registerUser`**: this method re-throws `RuntimeException` on email-dispatch failure instead of swallowing it — documented, not "fixed". |
| `isAccountVerified` | AuthService | `AuthServiceTest.java` | 3 | 1/0/2 | Returns `false` (not an exception) both when the user doesn't exist and when `isVerified` is `null` — same external behaviour, different internal reason; worth knowing if a caller ever needs to distinguish "no such user" from "unverified". |
| `loadUserByUsername` | UserDetailsServiceImpl | `UserDetailsServiceImplTest.java` | 2 | 1/1/0 | Authorities are built as `"ROLE_" + roleName` — asserted directly. |
| | | **Sub-total** | **19 executed + 1 disabled** | **6/7/4** | |

**AUTHENTICATION module running total (§3.1 + §3.2 + §3.2b):** 30 tests executed, 30 passed, 0 failed, 1 `@Disabled` (documented gap) — verified via `cd hango-backend && mvnw test -Dtest=AuthServiceTest,UserDetailsServiceImplTest` (all green, 2026-07-14).

---

### 3.3 UTC design for: `AdminController.updateUserStatus` (RBAC)

| Code Module | Role & Permission (RBAC) | Method | `updateUserStatus` |
|---|---|---|---|
| Created By | _(TBD)_ | Executed By | _(TBD)_ |
| Test requirement | Admin locks/unlocks a target account; must reject self-lock and non-Admin callers. | | |
| Passed / Failed / Untested | 0 / 0 / 10 | N / A / B | 4 / 4 / 2 |

```
UTC ID:       UTCID01     UTCID02     UTCID03     UTCID04     UTCID05     UTCID06     UTCID07     UTCID08     UTCID09     UTCID10
----------------------------------------------------------------------------------------------------------------------------------
Precondition:
 - Caller role | ADMIN |   ADMIN   |   LEARNER |   ADMIN   |   ADMIN   |   ADMIN   |   ADMIN   |   ADMIN   |   ADMIN   |   ADMIN   |
 - Target id | valid |   valid   |   valid   |  not found|   valid   |   valid   |   valid   |   valid   | = caller* |   valid   |
----------------------------------------------------------------------------------------------------------------------------------
Inputs:
 - status    |INACTIVE| "active" |  ANY      |  ANY      | "ACTIVE"  | "banned"  | "xyz123"  |   null    | "INACTIVE"|256-char   |
----------------------------------------------------------------------------------------------------------------------------------
Confirm / Expected Result:
 - Exp HTTP  |  200  |    200    |    403*** |    404    |    200    |   200**   |   200**   |   400     |  400***   |   400/500 |
 - Saved status|"INACTIVE"|"ACTIVE"|  N/A    |    N/A    | "ACTIVE"  |"BANNED"** |"XYZ123"** |    N/A    | unchanged***| N/A      |
----------------------------------------------------------------------------------------------------------------------------------
Case Type:   |   N   |     N     |     A     |     A     |     N     |     B     |     B     |     A     |     B     |     B     |
Status:      | Untested | Untested | Untested | Untested | Untested | Untested | Untested | Untested | Untested | Untested |
Exec Date:   |   —   |     —     |     —     |     —     |     —     |     —     |     —     |     —     |     —     |     —     |
Defect ID:   |   —   |     —     |     —     |     —     |     —     |     —     |     —     |     —     |     —     |     —     |
```

`*` UTCID09 = Admin targets their **own** account id.
`**` UTCID06/07: current code has **no whitelist** on `status` — any string is uppercased and saved as-is (`user.setStatus(status.toUpperCase())`). Expected HTTP 200 reflects *actual* code today; if the team wants a fixed `AccountStatus` enum (Active/Locked, per `HanGo_Documentation.md` §12), this is the gap to raise.
`***` UTCID03/09: `@PreAuthorize("hasRole('ADMINISTRATOR')")` covers UTCID03 (non-admin caller → 403, framework-level, should already pass). **UTCID09 is expected to FAIL against current code** — there is no self-lock guard in `AdminController.updateUserStatus`, contradicting `doc/specs/03-rbac.md` §4 ("Self-Locking... Return HTTP 400") and the still-unchecked TODO item under FE-03 QA. Write this test now (expect it red) so it becomes the acceptance check once the guard is implemented — do not soften the assertion to match today's (wrong) behavior.

---

### 3.4 UTC design for: `ExamService.saveExamAttempt`

| Code Module | Exam Management | Method | `saveExamAttempt` |
|---|---|---|---|
| Created By | _(TBD)_ | Executed By | _(TBD)_ |
| Test requirement | Persist a Learner's exam submission (score + answers JSON), compute attempt number, return DTO. | | |
| Passed / Failed / Untested | 0 / 0 / 10 | N / A / B | 4 / 4 / 2 |

```
UTC ID:       UTCID01     UTCID02     UTCID03     UTCID04     UTCID05     UTCID06     UTCID07     UTCID08     UTCID09     UTCID10
----------------------------------------------------------------------------------------------------------------------------------
Precondition:
 - Exam ID   | valid |   valid   |  invalid  |   valid   |   valid   |   valid   |   valid   |   valid   |   valid   |   valid   |
 - User ID   | valid |   valid   |   valid   |  invalid  |   valid   |   valid   |   valid   |   valid   |   valid   |   valid   |
----------------------------------------------------------------------------------------------------------------------------------
Inputs (request.getScore() is taken as-is — NOT recomputed server-side from correct answers, see note*):
 - Score     | 8.5   |   10.0    |   7.0     |   7.0     | -1.0      | 11.5      | 0.0       | 8.5       | 8.5       | 8.5       |
 - Answers   | 40 items| 40 items| 40 items  | 40 items  | 40 items  | 40 items  | 40 items  | empty map | null      | malformed |
----------------------------------------------------------------------------------------------------------------------------------
Confirm / Expected Result:
 - Exp Return| DTO   |   DTO     |   Error   |   Error   |   DTO**   |   DTO**   |   DTO     |   DTO     |   DTO     |   DTO     |
 - Exception |  None |    None   |RuntimeEx  |RuntimeEx  |    None** |    None** |    None   |   None    |   None    |   None    |
 - answersJson saved| enriched| enriched|  N/A  |    N/A    | enriched  | enriched  | enriched  | "{}" (catch)| "{}" (catch)| "{}" (catch)|
 - attemptNumber|   n+1 |    n+1    |    N/A    |    N/A    |   n+1     |   n+1     |   n+1     |   n+1     |   n+1     |   n+1     |
----------------------------------------------------------------------------------------------------------------------------------
Case Type:   |   N   |     B     |     A     |     A     |     B     |     B     |     N     |     B     |     A     |     A     |
Status:      | Untested | Untested | Untested | Untested | Untested | Untested | Untested | Untested | Untested | Untested |
Exec Date:   |   —   |     —     |     —     |     —     |     —     |     —     |     —     |     —     |     —     |     —     |
Defect ID:   |   —   |     —     |     —     |     —     |     —     |     —     |     —     |     —     |     —     |     —     |
```

*`*` **Known risk to flag, not a spec assumption**: `ExamService.saveExamAttempt` persists `request.getScore()` directly — it does **not** recompute the score server-side from the exam's stored correct answers. A malicious/buggy client can submit any score (including `-1.0` or `11.5`, outside the documented 0–10 scale in `HanGo_Documentation.md` BR-EXM-01). Current code has no server-side validation range check either — `**` marks UTCID05/06 where the out-of-range score is still accepted and saved as-is (expected to currently **pass** against real code, which is itself the defect to report). This is exactly the kind of finding this plan exists to surface — write the test to assert *current* behavior, then file it as a defect for the dev team (client-trust / missing score-range validation), rather than asserting the "should be" behavior and calling it a false failure.*

---

### 3.5 UTC design for: `PaymentService.processVnpayIpn` — **[SUPERSEDED — see below]**

> **Update 2026-07-24:** the actual implementation is PayOS, not VNPay (`PaymentServiceImpl.handlePayOSWebhook`), and it is now **Implemented and tested** (13 tests) — see `doc/specs/utc-sheet-payment.csv` for the real, current flat-CSV UTC list (idempotency, signature verification, revenue split, auto-enroll are all covered there). The wide/transposed design table below is kept only as **historical design reference** for the original VNPay-based acceptance criteria drafted before any code existed — per the tester's standing rule against reintroducing the wide/transposed format for new work (rejected twice already — flat one-row-per-testcase CSV is the only format going forward), do not extend or reuse this table's layout going forward.

| Code Module | Payment & Revenue | Method | `processVnpayIpn` (superseded by PayOS `handlePayOSWebhook`) |
|---|---|---|---|
| Created By | _(TBD)_ | Executed By | _(TBD)_ |
| Test requirement | Verify VNPay IPN checksum + amount, mark Order Paid idempotently, trigger auto-enroll + revenue split. **Design-only — `PaymentService`/`Order`/`RevenueRecord` do not exist in code yet; this UTC is the acceptance spec to build against**, per `doc/specs/12-payment-revenue.md` and BR-PAY-01..04. | | |
| Passed / Failed / Untested | 0 / 0 / 10 | N / A / B | 3 / 5 / 2 |

```
UTC ID:       UTCID01     UTCID02     UTCID03     UTCID04     UTCID05     UTCID06     UTCID07     UTCID08     UTCID09     UTCID10
----------------------------------------------------------------------------------------------------------------------------------
Precondition:
 - Order     |Pending|   Pending |   Pending |   Pending |   Paid*   |  Pending  |  Pending  |  Pending  |  Pending  |  Pending  |
----------------------------------------------------------------------------------------------------------------------------------
Inputs:
 - Checksum  | valid |   valid   |  INVALID  |   valid   |   valid   |   valid   |   valid   |   valid   |   valid   |   valid   |
 - Amount vs Order| match| match |    match  | MISMATCH  |   match   |   match   |   match   |   match   |   match   |   match   |
 - vnp_ResponseCode| "00" | "00" |    "00"   |    "00"   |    "00"   |  "24"(fail)| "00"     |    "00"   |    "00"   |    "00"   |
 - Duplicate IPN (same txn sent twice)|No| No|     No    |     No    |  Yes**    |     No    |     No    |     No    |     No    |    No     |
----------------------------------------------------------------------------------------------------------------------------------
Confirm / Expected Result:
 - Order status|  Paid |   Paid    | unchanged | unchanged |   Paid    |  Failed   |   Paid    |   Paid    |   Paid    |   Paid    |
 - Enrollment created|Yes| Yes  |     No    |     No    |No (already)|    No     |    Yes    |    Yes    |    Yes    |    Yes    |
 - RevenueRecord split| 70/30(Teacher)|60/40(Tutor)|N/A|N/A|No (already)|   N/A     |70/30      |60/40      |70/30      |70/30      |
 - HTTP response to VNPay| 200 |   200     |   200*** |   200***  |    200    |    200    |    200    |    200    |    200    |    200    |
----------------------------------------------------------------------------------------------------------------------------------
Case Type:   |   N   |     N     |     A     |     A     |     B     |     N     |     N     |     N     |     N     |     N     |
Status:      | Untested | Untested | Untested | Untested | Untested | Untested | Untested | Untested | Untested | Untested |
Exec Date:   |   —   |     —     |     —     |     —     |     —     |     —     |     —     |     —     |     —     |     —     |
Defect ID:   |   —   |     —     |     —     |     —     |     —     |     —     |     —     |     —     |     —     |     —     |
```

`*` UTCID05 = Order already `Paid` when a second IPN arrives — this is the idempotency case (NFR-03/FR-PAY-03).
`**` Duplicate delivery of the exact same VNPay transaction, distinct from a legitimately-Pending order.
`***` Must still return HTTP 200 to VNPay even on internal rejection (checksum/amount failure) — returning non-200 causes VNPay to retry; the *rejection* is internal (log + do not mutate Order), not surfaced as an HTTP error to the gateway.

---

### 3.6 UTC design for: `LearningPathwayService.generatePathway`

| Code Module | Recommendation / Learning | Method | `generatePathway` |
|---|---|---|---|
| Created By | _(TBD)_ | Executed By | _(TBD)_ |
| Test requirement | Build a learning roadmap from the student's latest Exam Attempt; enforce ownership; fall back gracefully when no published courses exist. | | |
| Passed / Failed / Untested | 0 / 0 / 8 | N / A / B | 4 / 3 / 1 |

```
UTC ID:       UTCID01     UTCID02     UTCID03     UTCID04     UTCID05     UTCID06     UTCID07     UTCID08
---------------------------------------------------------------------------------------------------------
Precondition:
 - studentId | valid     | valid     | invalid   | valid     | valid     | valid     | valid     | valid     |
 - examAttemptId (DTO)| valid | valid | valid    | invalid   | valid     | valid     | valid     | valid     |
 - Attempt.student == studentId| Yes | Yes    |    N/A    |    N/A    |    No     |    Yes    |    Yes    |    Yes    |
 - Published courses in DB| 5     | 5         |    N/A    |    N/A    |    N/A    |     0     |     5     |     5     |
---------------------------------------------------------------------------------------------------------
Inputs:
 - Gemini response|  Normal   |  Normal   |    N/A    |    N/A    |    N/A    |    N/A    |  Timeout  |  Malformed|
---------------------------------------------------------------------------------------------------------
Confirm / Expected Result:
 - Exp Return|   DTO     |   DTO     |ApiException 404|ApiException 404|ApiException 403|DTO (fallback msg)|DTO or Error*|DTO or Error*|
 - Uses fallback-to-all-courses|  No   |    No     |    N/A    |    N/A    |    N/A    |   Yes (empty pathway)|   No  |    No     |
---------------------------------------------------------------------------------------------------------
Case Type:   |   N   |     N     |     A     |     A     |     A     |     B     |     A     |     A     |
Status:      | Untested | Untested | Untested | Untested | Untested | Untested | Untested | Untested |
Exec Date:   |   —   |     —     |     —     |     —     |     —     |     —     |     —     |     —     |
Defect ID:   |   —   |     —     |     —     |     —     |     —     |     —     |     —     |     —     |
```

`*` UTCID07/08 depend on how `GeminiClientService` surfaces provider errors — **verify** the exact exception/fallback path when writing the test (mock `GeminiClientService` and inspect actual current handling; do not assume a fallback exists unless confirmed in code).

---

*Kế hoạch này bao phủ 18 method đại diện / ~102 UTC cho đợt đầu. Các method còn lại trong §1 (đã Implemented nhưng chưa lên UTC) và các module Planned sẽ được bổ sung UTC theo cùng khuôn mẫu ở các đợt tiếp theo — không mở rộng phạm vi tài liệu này quá mức cần thiết cho một kế hoạch ban đầu.*
